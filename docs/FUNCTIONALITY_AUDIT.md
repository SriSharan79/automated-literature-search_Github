# Functionality Audit — Desktop UI vs. Backend Capabilities

_Last updated: 2026-07-18._

This document compares what the desktop app (`alr.ui.desktop.main_window`) currently
exposes against the capabilities that already exist in the codebase but are **not**
reachable from the UI. It is meant as a backlog for surfacing existing value.

**Session state:** the tool **starts where it was left off** — the storage-path fields (collect, analyze, visualize, evaluate), their custom-path flags, and the Research Area/Question inputs are saved to `ui_session_state.json` when the window closes and restored on the next launch.

## 1. Currently in the desktop UI

| Tab / area | Functionality | Backend entry points |
|---|---|---|
| **1. Collect Literature** | Enter Research Area / Question; derive scope via LLM — the scope is now **optional** (a "Use scope in next steps" checkbox; unticked or blank, keyword suggestion and phrase generation run without it and the `RS_Rank` similarity column is skipped so it can't pollute `TOTAL_Rank`). Keywords live in an **in-tab check table** (Use ☑ + Keyword columns) instead of the old refinement pop-up: "Suggest Keywords via LLM" appends the suggestions (checked, deduplicated), rows can be **added manually** (comma-separated entry), removed, edited in place (double-click) and toggled, or **imported from a previously generated file** ("Import from File..." — keywords from a `*_keywords_list.json` (newest entry's `generated_keywords`) or any keyword-column spreadsheet; phrases from a `*_search_phrase_list.xlsx`/CSV with their `*_Rank` value) so past results can be re-displayed, re-checked and re-searched; a **"Select all" checkbutton** above each table checks/unchecks every row and mirrors the per-row state; "Process Selected Keywords → Search Phrases" always processes exactly the **checked** rows, with a **determinate per-subset progress bar** — and **Cancel is graceful**: it stops any further per-subset LLM calls but the phrases already collected are still ranked, logged and loaded into the phrase table exactly like a full run. Generated phrases land in a second **check table** (Use ☑ + Rank + Search Phrase): sorted by the selected ranking strategy (4 strategies: RA, RQ, RA+RQ, total — switching the radio or the top-N spinbox re-ranks the table), the top-N pre-checked; the **Rank heading is clickable to sort the table by numeric rank value** (ascending, click again for descending; manual "-" rows always sink to the bottom), manual phrases addable (rank "-", they survive re-ranks); **Run Publication Search runs over the checked phrases** (generated + manual), "Save Ranking to Excel Only" keeps the classic top-N-by-rank export. The Collection Manager is bootstrapped by whichever action runs first, so the scope step is no longer a prerequisite. All passes (scope derivation, keyword suggestion, phrase generation, publication search) run on the **background worker with a progress dialog**, and the search shows a **determinate per-phrase bar**. Publication collection is now **multi-backend** (`publication_search.py`) with a **user-selectable primary**: a "Search backend" radio row lets the user run **OpenAlex first (Scholar as fallback)** — the default — or **Google Scholar first (OpenAlex as fallback)**; whichever is chosen, the other backend takes over automatically when the primary returns nothing or is blocked. OpenAlex is the official REST API (title/abstract/DOI/year/venue/authors; abstracts rebuilt from the inverted index; optional `ALR_OPENALEX_MAILTO` env var joins the polite pool); Google Scholar goes through `scholarly` — when Google blocks the scraper (CAPTCHA / `MaxTriesExceededException`) the rows collected so far are kept, Scholar goes on a **15-minute cooldown**, and the run continues on OpenAlex instead of hanging (CAPTCHAs are never bypassed). Both backends run behind a **thread-safe sliding-window client-side rate limiter** (`publication_search.RateLimiter`): OpenAlex defaults to 8 requests/second and Scholar to 1 request/3 seconds, each env-configurable (`ALR_OPENALEX_RATE_MAX`/`_WINDOW`, `ALR_SCHOLAR_RATE_MAX`/`_WINDOW`), replacing the old flat per-item sleeps. A phrase with no results is **skipped instead of aborting the whole run**, and every Excel row records its backend in the new **`Source` column** | `collection_ui`, `publication_search.collect_publications/search_openalex/scrape_scholar_data`, `run_scholarly` (`progress_callback=`, `backend=`), `Keywords_Processing_with_scope` |
| **2. Analyze Literature** | Analyze a PDF or a folder **per-document** (each file taken through all selected steps before the next): Docling sectioning, abstract + intro + **results & conclusion** analysis (results/conclusion/summary/overview/future-work/outlook sections identified by heading keywords, LLM extracts Results Mentioned / Limitations or Boundary Conditions / Summary of the Content / Future Work / Outlook, analyzed section names logged, registry `Results_Conclusion` pass column), reference extraction, table/image extraction; **choose which components to extract** (Sections incl. tables/images [required], Abstract, Introduction, Results & Conclusion, References, DOI/metadata, Classification, **Build Text + Vector DB (RAG)**); choose LLM provider + **model**; **skip fuzzy-title duplicates** before batch processing (logs skipped filenames); records **page count** in the registry (no page limit); **reuses one Docling converter across the batch**; **precheck-driven copy-or-execute** — before any expensive step it checks both the storage space and SQL and **copies existing results across instead of recomputing** (`analysis_precheck.document_status`); syncs each document into SQL immediately after analysis (`sync_one_document`, always latest); runs **data evaluation immediately after each abstract** (section-vs-abstract grounding → SQL `evaluation_score`/`evaluation_json`); classifies by **title and by abstract text** (SQL `classification` / `abstract_classification`); the per-document pass **reuses** whatever a previous dated file already holds (falling back to a fresh run when there is nothing to copy), so a re-run never silently pays for the same LLM call twice; at **finalization** it refreshes SQL and then runs a **completeness check** (`analysis_precheck.compute_space_gaps`) over the whole space — for each stage (title/abstract classification, abstract/introduction/results-&-conclusion evaluation, DOI/metadata, introduction/references extraction) it reports how many documents are **missing from SQL** and how many of those a **prior dated file / on-disk analysis JSON** could fill, then raises **one consolidated dialog** where the user picks per stage: *Reuse existing* (copy, no LLM cost), *Run fresh* (re-run the stage), or *Skip*; "Reuse" is only offered where something is actually reusable and is the default when it closes the whole gap, closing the dialog skips everything, and when SQL is already complete no dialog appears at all (the old blind start-of-run copy-vs-generate prompts are gone); auto-enriches metadata from **all download logs**; optionally builds the **text DB + FAISS vector DB** into the storage space when the RAG box is ticked (`rag_builders.db_manager.generate_databases` — now covers **all analyzed data**: abstract sections plus, when present, Introduction (Background / Motivation / Gaps & Limitations / RQs & Scope → `Introduction_DB/`) and Results & Conclusion (Results Mentioned / Limitations or Boundary Conditions / Summary of the Content / Future Work / Outlook → `Results_Conclusion_DB/`) section DBs, each with its own Excel/JSON/FAISS index and master-overview sheets); runs on a **background thread with progress + cancel**. **Every** pass — not just this one — ends by pruning the empty files/folders the managed trees created but nothing wrote to: each manager registers its root on construction (`file_manager.register_managed_folder`) and `_run_threaded` drains that registry through `artifact_cleanup.prune_touched_folders` once the pass returns, including after a failure or a cancel — and **every process run now goes through `_run_threaded`** (the RAG query, the Tab-1 scope/keyword/Scholarly passes included), each showing a **determinate progress bar** whenever its backend iterates items (`progress_callback(done, total, …)` wired through analysis, RAG build, DOI, classification, evaluation, master Excel, download-log, query and Scholarly passes). Folders holding real content, and the run's own root, are never removed | `Pdf_File_processor.process_pdf_mode_file` (`components=`, `eval_mode=`), `analysis_precheck`, `sql_store.sync_one_document`, `batch_dedup`, `data_evaluator.evaluate_document/evaluate_space` (`mode=`), `classify_runner.classify_document/classify_space/classify_abstract_space` (`mode=`), `doi_metadata.enrich_space_with_doi` (precheck skip/copy), `download_log_enrich.enrich_from_download_logs`, `rag_builders.db_manager.generate_databases`, `artifact_cleanup.prune_empty_artifacts` |
| **3. Visualize & Query** | Build vector DBs from analyzed data; run a RAG query report (**on the background worker** with a determinate bar: one tick per searched section plus the overview and harvest steps); **query-section checkboxes** (pick any mix of the 16 RAG sections — 7 abstract + 4 Introduction + 5 Results & Conclusion attributes; sections without a built vector DB are skipped with a console warning); before the report is built a **checkbox picker** asks which attributes to add as **columns** on the enriched overview (independent of what was searched, defaults to the queried sections); **file harvesting is opt-in** — by default NO files are copied into the query folder and the overview is enriched by reading the abstract / Introduction / Results & Conclusion JSONs **directly from the storage space**; a **"Harvest matched files" checkbox beside the top-k spinbox** restores the old copy-JSONs-next-to-the-report behaviour (`generate_query_report(harvest_files=)`); **user-set top-k** (matches fetched per section index, spinbox, default 50); **query target toggle** (selected storage space vs. the common DB); **Common Database frame** — combine any number of storage spaces into ONE queryable text + vector DB (like the app-wide SQL DB): pick the common DB folder (default `ALR_main_folder/00_Common_DB`), add source spaces one-by-one or scan a folder for them, then Build/Update runs on a background thread with progress + cancel; each space's documents come **from the SQL database when already linked** (fallback: the space's files), the build is **incremental** — documents already in the common DB are filtered out **before any per-document work** (known UUIDs/Titles/optionally Filenames cost nothing beyond a log/manifest row: no JSON reads, no progress-loop iterations, no re-embedding), tracked in `Common_DB_manifest.xlsx` with a per-document `Sections` column (pre-manifest common DBs are adopted from their section Excels with per-section credit) — the vector sync only embeds new Excel rows; **attribute checkboxes** choose which sections (of `ALL_RAG_SECTIONS` — abstract, Introduction and Results & Conclusion data; each document contributes only the sections its analysis files provide), and a later build with more attributes ticked (or analysis data that appeared later) copies **only the missing sections** of already-known documents ("extended"); configuration (folder, sources, filename matching, sections) persists in `common_db_config.json` | `rag_builders.db_manager.generate_databases`, `db_manager.build_common_database`, `query_executor.generate_query_report` |
| **4. Section Editor** | Restructure and edit section JSON (embedded + pop-out) | `section_rewriter_view.JSONRestructurerUI` |
| **5. Evaluation / 6. Enrichment** *(now two separate tabs)* | Evaluation and Enrichment each have their own scrollable tab; the shared storage-folder + LLM-service + embedding-engine inputs appear on **both** tabs, bound to the same variables so the copies always stay in sync. **Evaluation**: user picks any of four batch evaluation types — **substring grounding** (data_evaluator; both strings are lowercased and stripped of all whitespace before the containment check, so case and line-break/spacing differences don't break a match), **lexical overlap** (Jaccard/ROUGE/BLEU), **distance & structural alignment** (Levenshtein/WER), **cosine similarity** (embeddings; reuses existing per-section FAISS `.bin` indexes, creates them from the text DBs when missing, embeds directly otherwise — an existing index built with a **different embedding backend** is detected via its `.meta.json` sidecar/dimension and is **left untouched and not reused**: items are embedded directly with the current backend so both sides of every cosine always come from one embedding space, and every cosine output records the model that produced it in a **`cosine_model`** column/entry — item rows, Overview sheets, `metrics_json` and the sentence-detail JSON, carried over by the copy-mode harvest) — against any of **Abstract, Introduction and Results & Conclusion** (checkboxes); the metric kinds are **sentence-level**: the identified reference text is split into sentences, every extracted item is measured against **each sentence**, the complete record goes to a per-document JSON (`Metric_Sentence_Details/{uuid}_{target}_Sentence_Metrics.json`, incl. which sentence produced each best value and its text), the workbook section sheets hold **one row per extracted item** (usual UUID/Title/Filename columns + `Item` number + `Content`) with the **best value per metric** (min for Levenshtein/WER, max otherwise) **and a `<metric>_reference` column recording the reference sentence that produced that value**; re-runs replace a document's item rows in place even when the item count changes, and SQL gets only the workbook-level summary; in **copy mode** (the user's reuse choice) previously recorded metric data is **harvested instead of recomputed** — the sentence-detail JSON first, else the latest previous dated workbook whether it used the old wide layout or the new per-item one — and re-written into today's workbooks **in the new per-item format** (reference sentences carried over where they were recorded; the old wide format never stored them, so those cells stay empty), falling back to the SQL-covered skip and then to a fresh compute; results go to per-section eval workbooks + **one dated workbook per metric kind** (`{date}_..._Lexical/Distance/Cosine_Metrics.xlsx`) + a **combined metrics overview workbook** with all metric data, and to SQL (`evaluation_*`, `intro_evaluation_*`, `rescon_evaluation_*`, `metrics_json`); manual two-text comparison panel kept; plus **Custom Topic Classification** — the user supplies a Topic tag and comma-separated classification tags, the classification prompt is generated from them (`build_custom_classifier_prompt`), titles or abstracts are batch-classified into a dated `{date}_{Topic}_Classification.xlsx` workbook and a SQL column named after the topic (registered like `classification`/`abstract_classification`, COALESCE-preserved). **Enrichment**: re-run abstract/references, classify titles/abstracts, build master Excel DB, download-log enrichment, DOI extraction, single-title classification, question-scored classification | `data_evaluator.evaluate_space` (`target=abstract\|intro`), `metric_evaluator.evaluate_space_metrics`, `Lexical_Overlap_Metrics`, `Distance_w_Structural _Alignment`, `vector_db_updater.vectorize_strings/load_index_file/create_faiss_index_cosine`, `Folder_Data_Analyzer.process_abstract/process_references`, `classify_runner.classify_space/classify_abstract_space`, `master_excel_db_builder.build_master_excel_db`, `download_log_enrich.enrich_from_download_logs` |
| Top bar | **API Keys** manager; per-provider **model chooser** (live list); **Open Review Tool** (standalone Review app) | `LLM_Config.set_api_key/get_stored_api_key`, `llm_utils.select_model_interactive`, `review_app.open_review_app` |

Cross-cutting: the **LLM layer** (`llm_utils`) no longer uses thread-based timeouts — every
remote chat/embedding request carries a **native `requests` timeout** (a hung socket raises
cleanly instead of leaking an abandoned worker thread) and **bounded retry-with-backoff** on
429/5xx/network errors (`Retry-After` honoured) *before* any service switch. Cross-service
fallback is **per-call** (`allow_fallback=` on `llm_call`/`embedding_call`): still on by
default for chat but loud, **opt-in (default off) for embeddings** so fallback vectors from a
different embedding space can never silently enter a FAISS index. What actually served each
call (requested vs. used service, model, fallback, errors) is recorded and readable via
`llm_utils.get_last_call_info()`. All rate limiting goes through **one shared sliding-window
limiter** (10 requests/60 s across chat + embeddings) — the flat `time.sleep(3)`s, the
per-classifier `time.sleep(1.5)`s and the off-by-one window guard are gone.

The standalone **Review tool** (`alr.ui.desktop.review_app`, launched from the top bar or
`review_main.py`) additionally covers: storage-space detection (complete/partial), DB linking,
DOI enrichment, **title + abstract classification** and **data evaluation** per space (each
synced to SQL first), a **Document Inspector tab** (`common/document_inspector.py`) — search
one document by **UUID, Title or Filename** (user-picked mode); the lookup **always starts in
SQL** and every shown field carries its provenance; values empty in SQL are **auto-filled from
the storage space the document was synced from** (Results & Conclusion content only exists
there); a document never synced to SQL prompts for a storage space and is served from its
`Processed_file_registry.xlsx` + analysis JSONs instead; the **PDF is located** from its known
paths (`relative_path`, the space's `pdf_files/`), with a threaded **"Locate PDF in folder…"**
recursive search (all nested subfolders, progress + cancel, chooser when several copies exist)
and an Open button — a raw DB browser, read-only SQL, a **column-picker Excel export**
(tick the database columns to export — optionally per storage space — no SQL needed),
cross-space stats (incl.
abstract-classified/evaluated counts), custom overviews (field-picker, filters, grouped charts,
per-topic counts, saved templates, natural-language requests; exports match the previewed
table), and a built-in **Guide tab** explaining every feature with examples.

The CLI (`alr.ui.cli.pipeline`) exposes roughly the same Collect / Analyze / Visualize
surface (now with model selection), plus it is the only place the console API-key prompt runs.

## 2. Backend capabilities — surfaced vs. still pending

Most of the original backlog is now wired in. Status legend: ✅ surfaced, ⏳ pending.

| Capability | What it does | Where it lives | Status |
|---|---|---|---|
| **JSON section rewriter/editor** | Full Tkinter editor for restructuring section JSON | `ui/desktop/section_rewriter_view.py` (`JSONRestructurerUI`) | ✅ Tab 4 (Section Editor) + pop-out |
| **Analysis evaluation** | Build vector-similarity + structural alignment eval DBs | `analysis_evaluation/data_evaluator.py` (`generate_databases`) | ✅ Tab 5 → "Build Evaluation DBs" |
| **Lexical / overlap metrics** | Jaccard, ROUGE, BLEU between texts | `analysis_evaluation/Lexical_Overlap_Metrics.py` | ✅ Tab 5 → Evaluation (batch over a space) + manual "Compute Metrics" |
| **Edit-distance / structural alignment** | Levenshtein + Word Error Rate metrics | `analysis_evaluation/Distance_w_Structural _Alignment.py` | ✅ Tab 5 → Evaluation (batch over a space) + manual "Compute Metrics" |
| **Embedding cosine evaluation** | Cosine similarity of each extracted section item vs. the identified abstract/introduction text (FAISS index reuse/create) | `analysis_evaluation/metric_evaluator.py` (uses `vector_db_updater`) | ✅ Tab 5 → Evaluation → "Cosine similarity" |
| **Introduction evaluation** | Ground Background / Motivation / Gaps & Limitations / RQs & Scope in the identified introduction text (+ all metric kinds) | `data_evaluator.evaluate_space(target="intro")`, `metric_evaluator` | ✅ Tab 5 → Evaluation target "Introduction"; auto after batch analysis |
| **Publication classification** | Classify a paper title against the topic/question set | `publication_classification/title_classifier.classify_title`, `classify_runner.classify_space` | ✅ Tab 5 (single title) + Review tool (per space) |
| **Abstract-only / references-only analysis** | Cheaper re-run passes on an analyzed storage | `Folder_Data_Analyzer.process_abstract`, `process_references` | ✅ Tab 5 → "Re-run" buttons |
| **RA/KC-scoped query** | Query restricted to Research-Area / Key-Concept sections | `query_executor.generate_query_report_RA_KC` | ✅ superseded in the UI by Tab 3's per-section query checkboxes (tick only Research Areas + Key Concepts); function kept for scripts |
| **SQL-backed review over all storages** | `alr_analyzed_data.db` consolidates every storage; dashboards/exports | `common.sql_store.AnalyzedDataStore` | ✅ Review tool (Database + Overviews tabs) |
| **Master Excel DB builder** | Consolidate per-section DBs into one master workbook (one `Overview` sheet + one sheet per selected attribute) | `rag_builders/master_excel_db_builder.py` (`build_master_excel_db`, `section_keys=`) | ✅ Tab 5 → "Build Master Excel DB" — a **checkbox picker** (`_pick_attributes_dialog`) asks first which of the 16 analyzed attributes to include (abstract + Introduction + Results & Conclusion; defaults to the abstract set), and only the JSONs those attributes come from are read |
| **Question-scored classification sheets** | Score a publication against the full question set into sheets | `publication_classification/classify_runner.question_score_space` → `Classification_logic_with_Q's.classify_excel_data_to_sheets` | ✅ Tab 5 → "Run Question-Scored Classification" (on-demand only; source = registry `title` or download-log `Publication Name`; managed output) |
| **Query-result harvesting** | Copy the matching documents' analysis JSONs for a query and enrich the overview Excel with one column per selected attribute; batch enrich reports | `query_executor.harvest_query_resources`, `enrich_overview_with_abstracts` (`enrich_keys=`), `batch_enrich_reports` | ✅ runs inside every Tab-3 query — a **checkbox picker** asks which of the 16 attributes to add as columns (defaults to the sections being queried), and only those JSON kinds are harvested (`Abstract_Json_files` / `Introduction_Json_files` / `Results_Conclusion_Json_files`). The section key **is** the JSON field, which also fixed the abstract columns silently coming back empty (`Research_Areas`/`Key_Concepts` were looked up underscored) |

## 3. Notable observations

- **Section editor** (`section_rewriter_view.py`) is now wired as Tab 4 (embedded + pop-out);
  it is no longer orphaned.
- **Provider selectors** exist on Collect and Analyze tabs, but the Visualize/Query and
  Review tabs are pure vector/DB work and need no LLM key (verified: no `llm_call` in the
  `rag_builders` query path).
- **Two entry points** (desktop + CLI) call the same backend; new backend features should be
  exposed through shared helpers so both stay in parity.
- Several backend scripts still contain **hard-coded absolute paths** in `__main__` blocks
  (e.g. `/remotedata/...`, `U:\...`); these are dev drivers, not used by the UI, but worth
  parameterizing if promoted into the app.
