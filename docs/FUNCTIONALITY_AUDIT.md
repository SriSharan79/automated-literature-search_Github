# Functionality Audit — Desktop UI vs. Backend Capabilities

_Last updated: 2026-07-05._

This document compares what the desktop app (`alr.ui.desktop.main_window`) currently
exposes against the capabilities that already exist in the codebase but are **not**
reachable from the UI. It is meant as a backlog for surfacing existing value.

## 1. Currently in the desktop UI

| Tab / area | Functionality | Backend entry points |
|---|---|---|
| **1. Collect Literature** | Enter Research Area / Question; derive scope via LLM; suggest keywords via LLM; process keywords with scope; rank search phrases (4 strategies: RA, RQ, RA+RQ, total); run Scholarly search; save ranking to Excel | `collection_ui`, `search_phrase_generator_utils`, `run_scholarly`, `Keywords_Processing_with_scope` |
| **2. Analyze Literature** | Analyze a PDF or a folder **per-document** (each file taken through all selected steps before the next): Docling sectioning, abstract + intro + **results & conclusion** analysis (results/conclusion/summary/overview/future-work/outlook sections identified by heading keywords, LLM extracts Results Mentioned / Limitations or Boundary Conditions / Summary of the Content / Future Work / Outlook, analyzed section names logged, registry `Results_Conclusion` pass column), reference extraction, table/image extraction; **choose which components to extract** (Sections incl. tables/images [required], Abstract, Introduction, Results & Conclusion, References, DOI/metadata, Classification, **Build Text + Vector DB (RAG)**); choose LLM provider + **model**; **skip fuzzy-title duplicates** before batch processing (logs skipped filenames); records **page count** in the registry (no page limit); **reuses one Docling converter across the batch**; **precheck-driven copy-or-execute** — before any expensive step it checks both the storage space and SQL and **copies existing results across instead of recomputing** (`analysis_precheck.document_status`); syncs each document into SQL immediately after analysis (`sync_one_document`, always latest); runs **data evaluation immediately after each abstract** (section-vs-abstract grounding → SQL `evaluation_score`/`evaluation_json`); classifies by **title and by abstract text** (SQL `classification` / `abstract_classification`); for **Classification & Evaluation**, when prior dated data exists asks **once per category** whether to *copy from the previous `{date}` file* or *generate new* (fresh dated file, prior files kept); auto-enriches metadata from **all download logs**; optionally builds the **text DB + FAISS vector DB** into the storage space when the RAG box is ticked (`rag_builders.db_manager.generate_databases` — now covers **all analyzed data**: abstract sections plus, when present, Introduction (Background / Motivation / Gaps & Limitations / RQs & Scope → `Introduction_DB/`) and Results & Conclusion (Results Mentioned / Limitations or Boundary Conditions / Summary of the Content / Future Work / Outlook → `Results_Conclusion_DB/`) section DBs, each with its own Excel/JSON/FAISS index and master-overview sheets); **prunes empty files/folders**; runs on a **background thread with progress + cancel** | `Pdf_File_processor.process_pdf_mode_file` (`components=`, `eval_mode=`), `analysis_precheck`, `sql_store.sync_one_document`, `batch_dedup`, `data_evaluator.evaluate_document/evaluate_space` (`mode=`), `classify_runner.classify_document/classify_space/classify_abstract_space` (`mode=`), `doi_metadata.enrich_space_with_doi` (precheck skip/copy), `download_log_enrich.enrich_from_download_logs`, `rag_builders.db_manager.generate_databases`, `artifact_cleanup.prune_empty_artifacts` |
| **3. Visualize & Query** | Build vector DBs from analyzed data; run a RAG query report; **query-section checkboxes** (pick any mix of the 16 RAG sections — 7 abstract + 4 Introduction + 5 Results & Conclusion attributes; sections without a built vector DB are skipped with a console warning); **user-set top-k** (matches fetched per section index, spinbox, default 50); **query target toggle** (selected storage space vs. the common DB); **Common Database frame** — combine any number of storage spaces into ONE queryable text + vector DB (like the app-wide SQL DB): pick the common DB folder (default `ALR_main_folder/00_Common_DB`), add source spaces one-by-one or scan a folder for them, then Build/Update runs on a background thread with progress + cancel; each space's documents come **from the SQL database when already linked** (fallback: the space's files), the build is **incremental** — documents already in the common DB are filtered out **before any per-document work** (known UUIDs/Titles/optionally Filenames cost nothing beyond a log/manifest row: no JSON reads, no progress-loop iterations, no re-embedding), tracked in `Common_DB_manifest.xlsx` with a per-document `Sections` column (pre-manifest common DBs are adopted from their section Excels with per-section credit) — the vector sync only embeds new Excel rows; **attribute checkboxes** choose which sections (of `ALL_RAG_SECTIONS` — abstract, Introduction and Results & Conclusion data; each document contributes only the sections its analysis files provide), and a later build with more attributes ticked (or analysis data that appeared later) copies **only the missing sections** of already-known documents ("extended"); configuration (folder, sources, filename matching, sections) persists in `common_db_config.json` | `rag_builders.db_manager.generate_databases`, `db_manager.build_common_database`, `query_executor.generate_query_report` |
| **4. Section Editor** | Restructure and edit section JSON (embedded + pop-out) | `section_rewriter_view.JSONRestructurerUI` |
| **5. Evaluation / 6. Enrichment** *(now two separate tabs)* | Evaluation and Enrichment each have their own scrollable tab; the shared storage-folder + LLM-service + embedding-engine inputs appear on **both** tabs, bound to the same variables so the copies always stay in sync. **Evaluation**: user picks any of four batch evaluation types — **substring grounding** (data_evaluator), **lexical overlap** (Jaccard/ROUGE/BLEU), **distance & structural alignment** (Levenshtein/WER), **cosine similarity** (embeddings; reuses existing per-section FAISS `.bin` indexes, creates them from the text DBs when missing, embeds directly otherwise) — against any of **Abstract, Introduction and Results & Conclusion** (checkboxes); the metric kinds are **sentence-level**: the identified reference text is split into sentences, every extracted item is measured against **each sentence**, the complete record goes to a per-document JSON (`Metric_Sentence_Details/{uuid}_{target}_Sentence_Metrics.json`, incl. which sentence produced each best value), the workbooks keep only the **best value per item and metric** (min for Levenshtein/WER, max otherwise), and SQL gets only the workbook-level summary; results go to per-section eval workbooks + **one dated workbook per metric kind** (`{date}_..._Lexical/Distance/Cosine_Metrics.xlsx`) + a **combined metrics overview workbook** with all metric data, and to SQL (`evaluation_*`, `intro_evaluation_*`, `rescon_evaluation_*`, `metrics_json`); manual two-text comparison panel kept; plus **Custom Topic Classification** — the user supplies a Topic tag and comma-separated classification tags, the classification prompt is generated from them (`build_custom_classifier_prompt`), titles or abstracts are batch-classified into a dated `{date}_{Topic}_Classification.xlsx` workbook and a SQL column named after the topic (registered like `classification`/`abstract_classification`, COALESCE-preserved). **Enrichment**: re-run abstract/references, classify titles/abstracts, build master Excel DB, download-log enrichment, DOI extraction, single-title classification, question-scored classification | `data_evaluator.evaluate_space` (`target=abstract\|intro`), `metric_evaluator.evaluate_space_metrics`, `Lexical_Overlap_Metrics`, `Distance_w_Structural _Alignment`, `vector_db_updater.vectorize_strings/load_index_file/create_faiss_index_cosine`, `Folder_Data_Analyzer.process_abstract/process_references`, `classify_runner.classify_space/classify_abstract_space`, `master_excel_db_builder.build_master_excel_db`, `download_log_enrich.enrich_from_download_logs` |
| Top bar | **API Keys** manager; per-provider **model chooser** (live list); **Open Review Tool** (standalone Review app) | `LLM_Config.set_api_key/get_stored_api_key`, `llm_utils.select_model_interactive`, `review_app.open_review_app` |

The standalone **Review tool** (`alr.ui.desktop.review_app`, launched from the top bar or
`review_main.py`) additionally covers: storage-space detection (complete/partial), DB linking,
DOI enrichment, **title + abstract classification** and **data evaluation** per space (each
synced to SQL first), a raw DB browser, read-only SQL, a **column-picker Excel export**
(tick the database columns to export — optionally per storage space — no SQL needed),
cross-space stats (incl.
abstract-classified/evaluated counts), custom overviews (field-picker, filters, grouped charts,
per-topic counts, saved templates, natural-language requests; exports match the previewed
table), and a built-in **Guide tab** explaining every feature with examples.

The CLI (`alr.ui.cli.pipeline`) exposes roughly the same Collect / Analyze / Visualize
surface (now with model selection), plus it is the only place the console API-key prompt runs.

## 2. Backend capabilities — surfaced vs. still pending

Most of the original backlog is now wired in. Status legend: ✅ surfaced, ⏳ pending.

| Capability | What it does | Where it lives | Status |
|---|---|---|---|
| **JSON section rewriter/editor** | Full Tkinter editor for restructuring section JSON | `ui/desktop/section_rewriter_view.py` (`JSONRestructurerUI`) | ✅ Tab 4 (Section Editor) + pop-out |
| **Analysis evaluation** | Build vector-similarity + structural alignment eval DBs | `analysis_evaluation/data_evaluator.py` (`generate_databases`) | ✅ Tab 5 → "Build Evaluation DBs" |
| **Lexical / overlap metrics** | Jaccard, ROUGE, BLEU between texts | `analysis_evaluation/Lexical_Overlap_Metrics.py` | ✅ Tab 5 → Evaluation (batch over a space) + manual "Compute Metrics" |
| **Edit-distance / structural alignment** | Levenshtein + Word Error Rate metrics | `analysis_evaluation/Distance_w_Structural _Alignment.py` | ✅ Tab 5 → Evaluation (batch over a space) + manual "Compute Metrics" |
| **Embedding cosine evaluation** | Cosine similarity of each extracted section item vs. the identified abstract/introduction text (FAISS index reuse/create) | `analysis_evaluation/metric_evaluator.py` (uses `vector_db_updater`) | ✅ Tab 5 → Evaluation → "Cosine similarity" |
| **Introduction evaluation** | Ground Background / Motivation / Gaps & Limitations / RQs & Scope in the identified introduction text (+ all metric kinds) | `data_evaluator.evaluate_space(target="intro")`, `metric_evaluator` | ✅ Tab 5 → Evaluation target "Introduction"; auto after batch analysis |
| **Publication classification** | Classify a paper title against the topic/question set | `publication_classification/title_classifier.classify_title`, `classify_runner.classify_space` | ✅ Tab 5 (single title) + Review tool (per space) |
| **Abstract-only / references-only analysis** | Cheaper re-run passes on an analyzed storage | `Folder_Data_Analyzer.process_abstract`, `process_references` | ✅ Tab 5 → "Re-run" buttons |
| **RA/KC-scoped query** | Query restricted to Research-Area / Key-Concept sections | `query_executor.generate_query_report_RA_KC` | ✅ superseded in the UI by Tab 3's per-section query checkboxes (tick only Research Areas + Key Concepts); function kept for scripts |
| **SQL-backed review over all storages** | `alr_analyzed_data.db` consolidates every storage; dashboards/exports | `common.sql_store.AnalyzedDataStore` | ✅ Review tool (Database + Overviews tabs) |
| **Master Excel DB builder** | Consolidate per-section DBs into one master workbook | `rag_builders/master_excel_db_builder.py` (`build_master_excel_db`) | ✅ Tab 5 → "Build Master Excel DB" (new `build_master_excel_db` orchestrator over `save_to_db`) |
| **Question-scored classification sheets** | Score a publication against the full question set into sheets | `publication_classification/classify_runner.question_score_space` → `Classification_logic_with_Q's.classify_excel_data_to_sheets` | ✅ Tab 5 → "Run Question-Scored Classification" (on-demand only; source = registry `title` or download-log `Publication Name`; managed output) |
| **Query-result harvesting** | Copy matching PDFs/JSONs for a query; enrich overview with abstracts; batch enrich reports | `query_executor.harvest_query_resources`, `enrich_overview_with_abstracts`, `batch_enrich_reports` | ⏳ intentionally not surfaced — the Query tab already generates a per-query overview Excel, which covers the current need |

## 3. Notable observations

- **Section editor** (`section_rewriter_view.py`) is now wired as Tab 4 (embedded + pop-out);
  it is no longer orphaned.
- **Provider selectors** exist on Collect and Analyze tabs, but the Visualize/Query and
  Review tabs are pure vector/DB work and need no LLM key (verified: no `llm_call` in the
  `rag_builders` query path).
- **Two entry points** (desktop + CLI) call the same backend; new backend features should be
  exposed through shared helpers so both stay in parity.
- Several backend scripts still contain **hard-coded absolute paths** in `__main__` blocks
  (e.g. `/remotedata/...`, `U:\...`); these are dev drivers, not used by the UI, but worth
  parameterizing if promoted into the app.
