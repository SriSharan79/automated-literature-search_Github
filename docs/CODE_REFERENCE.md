# Code reference

*Generated by `scripts/gen_code_reference.py` on 2026-08-01 — do not edit by hand; re-run the script instead.*

**73 modules · 34 classes · 338 methods · 588 module-level functions.**

Every public and private definition in `src/` is listed. One-line descriptions come from the code's own docstrings; modules that have none get their purpose from the generator's `MODULE_PURPOSE` map.

For *how the pieces fit together*, read [USER_MANUAL.md](USER_MANUAL.md); for the exhaustive behaviour notes, [FUNCTIONALITY_AUDIT.md](FUNCTIONALITY_AUDIT.md).

---

## Collection layer

*Turning a research question into a ranked list of publications to read.* — `alr/collection/`

### `src/alr/collection/Phrase_Classification_Utils.py`

Scores and ranks generated search phrases against the research area/question.  
*171 lines · 0 classes · 3 functions*

| Function | Purpose |
| --- | --- |
| `Classification_of_Phrase(qa_prompt_template, Phrase)` | — |
| `Phrase_Processing(Phrase)` | — |
| `Classify_excel_data(file_path, column_name, output_column_name)` | Identifies keywords from a list within the text of a specified column in an Excel file and adds a new column listing the found keywords. |

### `src/alr/collection/collection_imports.py`

Read previously generated keyword / search-phrase files back into plain Python lists, so the Collect-Literature tab can re-display and re-use them.  
*165 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `_dedupe_preserve_order(items)` | — |
| `_pick_column(df, candidates)` | — |
| `read_keywords_record(path)` | Return ``(unique_keywords, last_selected)`` from a keywords file: * ``unique_keywords`` -- every distinct keyword the file records (across all timestamped entries: LLM-suggested + user-added + selected), in first-seen order. |
| `read_keywords_file(path)` | Backward-compatible helper: just the unique keyword list. |
| `read_phrases_file(path, rank_column)` | Return ``[(rank, phrase), ...]`` from a search-phrase workbook/CSV. |

### `src/alr/collection/collection_system_prompts.py`

System prompts for scope generation, keyword suggestion and phrase generation.  
*218 lines · 0 classes · 0 functions*

### `src/alr/collection/keyword_sorting_Utils.py`

Sorts and de-duplicates keyword lists before phrase generation.  
*115 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `get_all_non_empty_subsets(input_list)` | Generate all non-empty subsets of the given input list. |
| `get_subsets_with_min_size(input_list, min_size)` | Generate all non-empty subsets of the given input list with a minimum size. |
| `get_pairwise_subsets(list1, list2)` | Generates subsets (pairs) that contain exactly one element from list1 and one element from list2. |
| `get_triplewise_subsets(list1, list2, list3)` | Generates subsets (triplets) that contain exactly one element from list1, one from list2, and one from list3. |
| `get_subsets_of_size(input_list, num)` | Generate all subsets of the given input list with the exact size of 'num'. |

### `src/alr/collection/publication_classifier.py`

Classify a collected publications list against the user's keyword tags.  
*113 lines · 0 classes · 2 functions*

| Function | Purpose |
| --- | --- |
| `_valid_text(value)` | — |
| `classify_publications_list(CM, tags, service, progress_callback, should_cancel)` | Classify the publications in ``CM.publications_list_excel`` against ``tags`` (the user's selected keywords) and write a dated classified workbook into the collection space's ``classified_publications/`` folder. |

### `src/alr/collection/publication_search.py`

Publication-search backends for the collection pipeline.  
*383 lines · 1 classes · 9 functions*

**class `RateLimiter`** — Thread-safe sliding-window limiter: at most ``max_requests`` per ``window_seconds``.

| Method | Purpose |
| --- | --- |
| `__init__(self, max_requests, window_seconds, name)` | — |
| `acquire(self)` | — |

| Function | Purpose |
| --- | --- |
| `_env_float(name, default)` | — |
| `find_keywords_in_phrase(text, keywords)` | — |
| `scholar_available()` | True when the Scholar backend is not sitting out a block cooldown. |
| `mark_scholar_blocked(reason)` | Put Scholar on cooldown after Google blocked the scraper. |
| `_reconstruct_openalex_abstract(inverted_index)` | OpenAlex stores abstracts as an inverted index {word: [positions]}; rebuild the plain text from it. |
| `_openalex_get(params, timeout, max_retries)` | GET from OpenAlex with a native timeout and bounded retry on 429/5xx. |
| `search_openalex(search_query, Num_Results, Total_keywords)` | Search the OpenAlex API and return rows in the same shape as the Scholar scraper (the publications-Excel COLUMNS), plus 'Source': 'OpenAlex'. |
| `scrape_scholar_data(search_query, Num_Results, Total_keywords)` | Searches Google Scholar for publications matching the combined keywords and returns the data as a list of dictionaries (plus 'Source' column). |
| `collect_publications(search_query, Num_Results, Total_keywords, backend)` | Collect publications for one search phrase. |

### `src/alr/collection/search_phrase_generator_config.py`

Tunables for the search-phrase generator (counts, ranking bases, output columns).  
*83 lines · 0 classes · 0 functions*

### `src/alr/collection/search_phrase_generator_logger.py`

Writes the per-search keyword/phrase workbooks and their dated history.  
*265 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `_ensure_parent_dir(path)` | Make sure the folder holding ``path`` exists before an Excel/JSON write. |
| `Log_keyPhrases(Key_Phrases, EXCEL_FILE_PATH)` | Handles the core logic: loading existing data, performing deduplication, updating occurrence counts, appending new search phrases, and saving the file. |
| `log_Keyword_Json(CM)` | Logs research parameters and results into a JSON file with a timestamp. |
| `log_generated_list_file(filename, Count, EXCEL_FILE_PATH, CM)` | — |
| `aggregate_and_update_excel(new_publications, EXCEL_FILE_PATH)` | Handles the core logic: loading existing data, performing deduplication, updating occurrence counts, appending new search phrases, and saving the file. |

### `src/alr/collection/search_phrase_generator_utils.py`

Builds search phrases from selected keywords and ranks them four ways.  
*350 lines · 0 classes · 6 functions*

| Function | Purpose |
| --- | --- |
| `generation_of_Key_phrases_with_scope(formatted_user_prompt, phrase_excel_file, Keywords, Total_keywords, llm)` | — |
| `Keywords_Processing_with_scope(CM, should_cancel, progress_callback)` | Generate search phrases per keyword subset via the LLM, then rank and log them. |
| `rank_to_available_data(CM)` | — |
| `preprocess(text)` | Safe preprocess: handles NaN, float, None, empty. |
| `rank_search_phrases(scope, column_name, excel_file, score_column_name, rank_column_name, top_k, threshold)` | — |
| `run_scholarly(Input_Phrases, CM, Num_Search_Results, progress_callback, backend)` | Loops through the Input_Phrases, collects publications for each phrase via collect_publications() (OpenAlex primary, Google Scholar fallback with block cooldown), and updates the Excel file. |

## Analysis layer

*Turning a PDF into structured, per-document analyzed data.* — `alr/data_analysis/`

### `src/alr/data_analysis/Abstract_Analyzer.py`

Extracts the seven abstract attributes from an identified abstract via the LLM.  
*188 lines · 0 classes · 3 functions*

| Function | Purpose |
| --- | --- |
| `check_and_log_data(json_file_path, excel_file_path, ID, time_taken)` | Behaviour: - Exactly ONE row per UUID. - If UUID exists: overwrite/update ALL columns for that UUID (even if not empty/NA). - If UUID does not exist: append a new row. |
| `get_abstract_text(MF)` | — |
| `analyze_abstract(ID, MF)` | — |

### `src/alr/data_analysis/Data_analysis_system_prompts.py`

System prompts for abstract, introduction, results & conclusion and reference extraction.  
*303 lines · 0 classes · 0 functions*

### `src/alr/data_analysis/Data_sorting_utils.py`

Sorts Docling chunks into named sections and writes the per-document section logs.  
*414 lines · 0 classes · 10 functions*

| Function | Purpose |
| --- | --- |
| `extract_chunk_heading(chunk)` | Handles extraction from both raw Pydantic/Docling models and cached dictionaries. |
| `get_actual_text_content(doc_item, doc)` | Safely retrieves text content. Fallback matches cached raw data patterns when text is already merged or when doc tree is out of range. |
| `handle_text_label(actual_text_content, text_in_chunk, last_text_item_index, processed_document_texts, previous_Heading, chunk_heading)` | — |
| `handle_list_item_label(actual_text_content, text_in_chunk, last_text_item_index, processed_document_texts, previous_Heading, chunk_heading)` | — |
| `process_document_chunks(chunks, doc)` | — |
| `sort_raw_text_with_headings(chunks, doc)` | — |
| `merge_content_by_refined_headings(processed_document_texts, refined_headings)` | Merges text blocks with invalid headings into the previous valid block. |
| `merge_content_by_refined_headings_Chunks(Sections_Raw_Chunk_text, refined_headings)` | Merges text blocks with invalid headings into the previous valid block and returns a structured JSON-style list of dictionaries. |
| `categorize_sections(refined_Sections_Raw_chunks, final_merged_content, body_headings, excel_data)` | Maps content to standard columns and includes the specific chunk list for each section in the final JSON. |
| `save_excel_log(excel_log_path, excel_data, Time_taken_4_chunking, Time_taken_4_section_processing)` | Handles persistence of the Excel log file. |

### `src/alr/data_analysis/File_Data_extraction_with_Docling.py`

Runs Docling over one PDF and caches its chunks, with a per-file timeout.  
*396 lines · 0 classes · 11 functions*

| Function | Purpose |
| --- | --- |
| `_get_chunker()` | Lazily create and cache the Docling HybridChunker on first use. |
| `process_llm_refined_structure(llm_output_string)` | — |
| `get_llm_refined_lists(unique_headings_list, llm_service)` | Handles the two-step LLM refinement process. |
| `reference_pipeline_worker(chunks, doc, ref_json_path, excel_log_path, pdf_name, ID)` | Helper function to run the extraction and logging sequentially inside a background thread. |
| `_init_excel_data(file_path, ID)` | — |
| `_excel_log_has_file(excel_path, pdf_name, filename_col_candidates)` | Returns True if excel_path exists and contains a row whose 'file name' column matches pdf_name. |
| `_decide_skip_paths(Main_Folder, pdf_name)` | Checks both raw_section log and reference Excel log for this pdf_name. |
| `_extract_and_chunk(MF, logger, file_path, start_time, doc_converter)` | — |
| `_process_sections(chunks, doc, llm_service)` | — |
| `_categorise_sections(refined_Sections_Raw_chunks, final_merged_content, body_headings, excel_data)` | — |
| `_save_section_outputs(processed_data_for_json, Main_Folder, excel_data, time_taken_4_chunking, time_taken_4_section_processing, pdf_name)` | — |

### `src/alr/data_analysis/Folder_Data_Analyzer.py`

CLI batch driver: analyze every PDF in a folder, then sync the space into SQL.  
*169 lines · 0 classes · 4 functions*

| Function | Purpose |
| --- | --- |
| `_as_path(p)` | Convert str/pathlike/Path into Path safely. |
| `process_abstract(MF, progress_callback)` | — |
| `process_references(MF, progress_callback)` | — |
| `process_folder(source_path, storage_path, n)` | — |

### `src/alr/data_analysis/Introduction_Analyzer.py`

Extracts Background / Motivation / Gaps & Limitations / RQs & Scope from the introduction.  
*185 lines · 0 classes · 3 functions*

| Function | Purpose |
| --- | --- |
| `check_and_log_data(json_file_path, excel_file_path, ID, time_taken)` | Behaviour: - Exactly ONE row per UUID. - If UUID exists: overwrite/update ALL columns for that UUID (even if not empty/NA). - If UUID does not exist: append a new row. |
| `get_Introduction_text(MF)` | — |
| `analyze_Introduction(ID, MF)` | — |

### `src/alr/data_analysis/LLM_Reference_Extractor.py`

Parses the reference section's chunks into structured citation records.  
*307 lines · 0 classes · 11 functions*

| Function | Purpose |
| --- | --- |
| `_get_converter()` | Lazily create and cache the Docling DocumentConverter on first use. |
| `_get_chunker()` | Lazily create and cache the Docling HybridChunker on first use. |
| `has_dash_in_last_n(text, n)` | Check if '-' exists in last N chars (after stripping trailing whitespace). |
| `validate_input_data(chunks_list, output_path)` | Checks if doc or chunks are missing and initializes empty file if needed. |
| `is_reference_heading(heading)` | Checks if a heading matches reference keywords. |
| `get_smart_split_index(text, preferred_delim, fallback_delim)` | Identifies the best point to split long text. |
| `perform_core_llm_extraction(raw_text, output_path, llm_service)` | Base Case: Standard LLM Logic for text <= 800 chars. |
| `handle_llm_extraction(raw_text, output_path, llm_service)` | Recursive Orchestrator. Ensures no text block > 800 chars reaches the LLM. |
| `process_references_from_chunks(chunks, output_path, llm_service)` | Main pipeline for reference extraction. |
| `process_references_from_chunks_from_Sec_JSON(chunks, output_path, llm_service)` | Main pipeline for reference extraction. |
| `docling_process_references_file(file_path, ID, Main_Folder)` | — |

### `src/alr/data_analysis/Pdf_File_processor.py`

The per-document pipeline: sectioning, the three analyzers, references, logs and registry.  
*1087 lines · 2 classes · 26 functions*

**class `ChunkingResult`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, doc, chunks, end_time, time_taken)` | — |

**class `BatchExtractor`** — Docling extraction for a batch: the model is loaded once, in a worker process, and every file is bounded by ``timeout`` seconds.

| Method | Purpose |
| --- | --- |
| `__init__(self, timeout, enable_ocr, image_resolution_scale)` | — |
| `_start(self)` | — |
| `_kill(self)` | — |
| `close(self)` | Stop the worker (politely if it is idle, forcibly otherwise). |
| `__enter__(self)` | — |
| `__exit__(self, *_exc)` | — |
| `extract(self, file_path, start_time, MF)` | Extract one PDF. Raises TimeoutError past ``timeout``, or whatever the extraction itself raised. |

| Function | Purpose |
| --- | --- |
| `setup_pdf_file_logger(log_file_path)` | Creates, attaches, and returns a specific file logger handler. |
| `close_pdf_file_logger(f_handler)` | Safely closes and removes the file logger handler to prevent log leaks. |
| `_chunking_wrapper(queue, f_path, s_time, MF)` | Subprocess wrapper for extraction. |
| `_run_extraction_with_timeout(file_path, start_time, MF, timeout)` | Polls the queue to return data as soon as it is available. |
| `_batch_extractor_worker(req_q, res_q, enable_ocr, image_resolution_scale)` | Child process: build the Docling converter **once**, then extract on demand. |
| `_extract_chunks(file_path, start_time, MF, doc_converter, timeout)` | Run Docling extraction and return a :class:`ChunkingResult`. |
| `save_logs(new_success, success_path, new_failed, failed_path)` | Appends new results to existing Excel logs or creates new ones. |
| `_init_manager(storage_path)` | — |
| `_get_page_count(file_path)` | Return the PDF page count, or None if the file cannot be parsed. |
| `_load_registry(excel_success)` | — |
| `_ensure_registry_column(excel_path, column, default)` | Add a column to the processed-file registry when older registries lack it. |
| `_recheck_title_(excel_success, file_path, llm_service)` | — |
| `_is_skipped(file_path, current_title, processed_files, processed_titles, llm_service)` | — |
| `_register_success(new_success, uniq_id, file_path, current_title, formatted_time, completed_steps, pages)` | — |
| `_register_failure(new_failed, file_path, error_Msg, failed_dest_root)` | — |
| `process_pdf_sections(file, storage_path, doc_converter)` | — |
| `process_pdf_references(file, storage_path)` | — |
| `_refresh_attribute_log(MF, target, uuid)` | Rewrite the per-attribute A/NA log row after a top-up changed the JSON, so the analysis logs keep matching what is actually stored. |
| `_stored_analysis_missing(MF, target, pdf_name, uuid)` | True when the analysis log claims a document is done but the stored JSON is not on disk (deleted by an earlier error-token cleanup, pruned, or the space was moved without its log). |
| `_top_up_if_incomplete(MF, target, pdf_name, uuid)` | Cheap gap check for an already-analyzed document: read the stored analysis JSON and, when attributes are still empty, complete them in two ordered stages (see `section_resolver.complete_missing_attributes`): 1. |
| `process_pdf_abstract(file, storage_path)` | — |
| `process_pdf_intro(file, storage_path)` | — |
| `process_pdf_results_conclusion(file, storage_path)` | — |
| `process_pdf_file(file, storage_path)` | — |
| `_resolve_components(mode, components)` | Resolve which per-document components to run. |
| `process_pdf_mode_file(file, storage_path, mode, components, doc_converter, eval_mode, should_cancel)` | Orchestrates the PDF processing. mode='a': Only attempts Abstract (+ Introduction + Results & Conclusion) extraction. mode='r': Only attempts Reference extraction. mode=None: Runs the full pipeline (Sectioning -> Abstract -> Introduction -> Results & Conclusion -> References). ``components`` optionally overrides ``mode`` with an explicit selection among ``{'abstract', 'intro', 'results', 'refer… |

### `src/alr/data_analysis/Refrences_log_utils.py`

Reads and writes the extracted-references workbook.  
*288 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `check_complex_reference_sequence(json_file_path)` | — |
| `count_entry_types(json_file_path)` | — |
| `repair_truncated_json(raw_text)` | Robust repair for potentially truncated JSON arrays. |
| `save_references_to_json(ai_response_text, file_path)` | Parses the AI response and appends/saves it to a specific file path. |
| `log_Ref_data_extracted(excel_log_path, JSON_path, pdf_name, ID, Time_taken)` | — |

### `src/alr/data_analysis/Results_Conclusion_Analyzer.py`

Extracts the five Results & Conclusion attributes from the identified section text.  
*271 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `check_and_log_data(json_file_path, excel_file_path, ID, time_taken, analyzed_sections)` | Behaviour: - Exactly ONE row per UUID. - If UUID exists: overwrite/update ALL columns for that UUID (even if not empty/NA). - If UUID does not exist: append a new row. - Also records WHICH sections were analyzed (Sections_Analyzed column). |
| `get_results_conclusion_sections(MF)` | Scan the raw sections JSON for every section whose name contains a results/conclusion related keyword (Results, Conclusion, Summary, Overview, Future Work, Outlook, ...). |
| `get_results_conclusion_text(MF)` | Identify the results/conclusion content of the current document. |
| `_record_analyzed_sections(json_file_path, analyzed_sections)` | Add the list of analyzed section names into the stored analysis JSON. |
| `analyze_results_conclusion(ID, MF)` | — |

### `src/alr/data_analysis/Table_image_extractor.py`

Exports tables and figures from a PDF and owns the shared Docling converter.  
*324 lines · 1 classes · 3 functions*

**class `DoclingExtractor`** — Extract tables, images, and headings using Docling.

| Method | Purpose |
| --- | --- |
| `__init__(self, input_path, tables_output_path, images_output_path, enable_ocr, image_resolution_scale, doc_converter)` | — |
| `extract_all(self)` | Extract tables, images, and headings in one pass. |
| `_extract_tables(self, doc)` | Extract all tables and save as CSV files. |
| `_extract_images(self, doc)` | Extract all images with deduplication. Includes 'hash' field per entry. |
| `_extract_headings(self, doc)` | Extract section headings detected by Docling's layout analysis. |

| Function | Purpose |
| --- | --- |
| `build_doc_converter(enable_ocr, image_resolution_scale)` | Build a Docling ``DocumentConverter`` with the project's pipeline options. |
| `get_shared_doc_converter(enable_ocr, image_resolution_scale)` | Return a cached ``DocumentConverter``, building it on first use. |
| `bbox_to_dict(bbox)` | Convert Docling BoundingBox to JSON-serializable dict. |

### `src/alr/data_analysis/batch_dedup.py`

alr.data_analysis.batch_dedup ============================= Before batch-processing a folder of PDFs, detect documents whose *title* (as extracted from the PDF) fuzzy-matches a document that was **already analyzed** (recorded in the storage space's ``Processed_file_registry.xlsx``) or another PDF earlier in the same batch.  
*401 lines · 0 classes · 7 functions*

| Function | Purpose |
| --- | --- |
| `_normalize_title(title)` | Lowercase, strip punctuation and collapse whitespace for robust matching. |
| `_is_usable_title(title)` | A title is usable for matching only if it is present and specific enough. |
| `_load_scan_log(scan_log_path)` | Load the persistent dedup scan log into ``{filename: row-dict}``. |
| `_save_scan_log(scan_log_path, scanned_map, new_rows)` | Merge freshly scanned rows into the persistent scan log (latest wins) and write it. |
| `_components_complete(status_map, components)` | True if every requested component is already marked 'Passed' for a document. |
| `find_new_and_duplicate_pdfs(source_folder, storage_path, threshold, llm_service, components, progress_callback, should_cancel)` | Split the PDFs under ``source_folder`` into those to process and those to skip as duplicates. |
| `batch_process_folder(source_path, storage_path, skip_duplicates, threshold, mode, components, llm_service, progress_callback, should_cancel)` | Process every PDF under ``source_path`` with no page limit, optionally skipping duplicates first. |

### `src/alr/data_analysis/doi_metadata.py`

alr.data_analysis.doi_metadata ============================= DOI / publication-metadata extraction (relocated from the archive into the package).  
*678 lines · 1 classes · 3 functions*

**class `MetadataLogic`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self)` | — |
| `extract_pdf_info(self, file_path)` | Rückgabe: text (1st 3 pages), is_landscape (any page), num_pages, has_comments (any page) |
| `_clean_text(self, text)` | — |
| `fetch_metadata_by_doi(self, text)` | — |
| `fetch_metadata_by_arxiv(self, text, filename)` | — |
| `fetch_arxiv_batch(self, failed_list)` | Takes a list of (filename, arxiv_id) and fetches them in one go. |
| `extract_meta_data_from_doi(self, file_path)` | — |
| `_iter_pdf_files(self, input_path)` | Yield PDF file paths for ``input_path``. Accepts either a single PDF file path or a folder, which is walked recursively. This lets callers target exactly a selected file/folder instead of always scanning an entire storage-space PDF subfolder. |
| `process_input_to_excel(self, input_path, output_excel, should_cancel, skip_filenames, progress_callback)` | Process a single PDF file OR a folder of PDFs (recursively) and write the combined metadata to ``output_excel``. |
| `process_directory_to_excel(self, root_folder, output_excel, should_cancel, skip_filenames)` | Kept for backward compatibility: scans a folder recursively for PDFs. |
| `_save_to_excel(self, data_list, filename)` | Helper to convert list of dicts to Excel with specific column order |

| Function | Purpose |
| --- | --- |
| `_has_real_doi(value)` | True if a DOI/link value is present and not a 'not found' placeholder. |
| `_doi_row_from_sql(doc)` | Reconstruct a DOI_Metadata-shaped row from an existing SQL document row. |
| `enrich_space_with_doi(manager, db_path, should_cancel, input_path, progress_callback)` | Run DOI/metadata extraction, write the managed ``DOI_Metadata.xlsx``, and push the metadata into the SQLite store (matching by File_Name -> document filename). |

### `src/alr/data_analysis/section_resolver.py`

alr.data_analysis.section_resolver ================================== Shared section-location and attribute-completion logic for the three analysis targets (Abstract, Introduction, Results & Conclusion).  
*935 lines · 1 classes · 28 functions*

**class `TargetSpec`** — Everything that differs between the three analysis targets.

| Function | Purpose |
| --- | --- |
| `is_missing(value)` | True when an extracted attribute holds no usable information. |
| `missing_keys(data, spec)` | The attributes of `spec` that `data` does not (usefully) contain. |
| `_load_json(path)` | — |
| `_json_path(MF, spec)` | — |
| `_write_json(path, data, spec)` | Write `data` back to `path`, reporting (rather than raising) on failure. |
| `load_chunks(MF)` | Every chunk of the document, flattened into one document-ordered list. |
| `is_back_matter(section_name)` | True for the standard non-body sections that trail (or bracket) a paper: references, appendices, acknowledgements, funding, declarations and the like. |
| `load_body_chunks(MF)` | The document's chunks with the back-matter sections removed, so positional ratios are taken over the paper's body rather than over its bibliography. |
| `chunk_window(chunks, spec)` | The positional slice of `chunks` where `spec`'s content usually lives. |
| `_clip(text)` | — |
| `identify_via_window(MF, spec, llm_service)` | Scan the target's share of the document body and identify the content. |
| `resolve_section_text(MF, target, base_text, provenance)` | Return (section text, provenance) for `target`, extending whatever the analyzer's own matching already produced. |
| `_load_analyzer(spec)` | The target's ordinary `analyze_*(ID, MF) -> 'P'\|'F'` entry point. |
| `_resolve_document_id(MF, spec, doc_id)` | The UUID the analyzers expect as their first argument. |
| `reanalysis_attempts(data)` | — |
| `_merge_reanalysis(before, after, spec)` | Fold a re-analysis result into the stored one, gap-keys only. |
| `reanalyze_missing_attributes(MF, target, doc_id, force)` | Stage 1 of attribute completion: run the target's ORDINARY analyzer once more and merge whatever it manages to fill into the stored analysis. |
| `follow_up_text(MF, spec, section_text, count)` | The next `count` chunks after the ones the section text came from. |
| `top_up_attempts(data)` | — |
| `needs_top_up(MF, target)` | True when the stored analysis for `target` still has empty attributes and no top-up pass has been spent on it yet. |
| `top_up_missing_attributes(MF, target, force)` | Run ONE additional extraction pass for the attributes still missing from the stored analysis of `target`, widening the input by the next :data:`FOLLOW_UP_CHUNKS` chunks. |
| `needs_completion(MF, target)` | True when the stored analysis for `target` still has empty attributes AND at least one of the two completion stages is still unspent. |
| `_space_manager(MF)` | — |
| `_space_documents(MF)` | ``[(uuid, filename), ...]`` from the space's registry. |
| `scan_space(MF, targets)` | Report which documents of a storage space still have empty attributes and could be completed, **without doing any work**. |
| `complete_space(MF, targets, force, should_cancel, progress_callback, push_sql, db_path)` | Complete the missing attributes of a whole storage space **from what the space already holds** -- the per-document analysis JSONs and the raw sections JSON. |
| `_refresh_target_log(MF, target, uuid)` | Rewrite the per-attribute A/NA log row after a completion changed the JSON. |
| `complete_missing_attributes(MF, target, doc_id, force)` | Fill the attributes still missing from the stored analysis of `target`. |

### `src/alr/data_analysis/title_extracter.py`

Determines a PDF's real title (DOI/arXiv lookup, then LLM disambiguation), memoized per file.  
*388 lines · 0 classes · 11 functions*

| Function | Purpose |
| --- | --- |
| `extract_pdf_info(file_path)` | Rückgabe: text (1st 3 pages), is_landscape (any page), num_pages, has_comments (any page) |
| `_clean_text(text)` | — |
| `fetch_metadata_by_doi(text)` | — |
| `fetch_metadata_by_arxiv(text)` | — |
| `extract_meta_data_from_doi(file_path)` | — |
| `get_title_by_font_size(file_path)` | — |
| `get_title_metadata(file_path)` | — |
| `_title_key(file_path)` | — |
| `clear_title_cache()` | Forget every memoized title (tests, or a deliberate re-extraction). |
| `get_title_in_the_file(file_path, llm_service)` | — |
| `_extract_title_in_the_file(file_path, llm_service)` | — |

## RAG / database layer

*Turning analyzed data into searchable text + vector databases and reports.* — `alr/rag_builders/`

### `src/alr/rag_builders/db_cache.py`

alr.rag_builders.db_cache ========================= Process-wide write-behind cache for the per-section JSON databases.  
*104 lines · 0 classes · 7 functions*

| Function | Purpose |
| --- | --- |
| `active()` | True while a build scope is open (writes are batched). |
| `open_scope()` | — |
| `close_scope()` | Leave one scope level; the outermost one flushes and clears. |
| `get(json_path)` | The shared image of one JSON DB: {"data": list, "uuids": set, ...}. |
| `add(json_path, data_entry)` | Append ``data_entry`` unless its UUID is already registered. |
| `has(json_path, uuid)` | — |
| `flush()` | Write every dirty JSON DB out. Safe to call repeatedly. |

### `src/alr/rag_builders/db_manager.py`

Builds the per-section text and vector databases for a space, and the common multi-space DB.  
*1119 lines · 0 classes · 29 functions*

| Function | Purpose |
| --- | --- |
| `_all_rag_keys()` | Every RAG-buildable section key (abstract + intro + rescon), in order. |
| `_source_keys(source)` | The section keys one analysis JSON ('abstract'/'intro'/'rescon') provides. |
| `_read_json_dict(path)` | Load a JSON file expected to hold a dict; {} on any failure. |
| `_log_uuids(log_path)` | UUID column of an analysis log workbook; [] when it doesn't exist. |
| `_analysis_source_paths(MF, source)` | (log_path, per-uuid json_path getter) for an intro/rescon source. |
| `update_VDB_status(VDB, key, str_count, vec_count)` | — |
| `_excel_content_strings(excel_path)` | Read the "Content" column POSITIONALLY from a section Excel DB. |
| `_rebuild_section_index(VDB_path, strings)` | Force-rebuild one section's FAISS index from the given strings. |
| `_sync_sections_VDB(VDB, sections, rebuild, rebuild_text_db)` | Sync each section's FAISS index against its EXCEL DB "Content" column (previously the section JSON — which could drift ahead of the Excel and left index.ntotal > Excel rows with no way to heal). |
| `_sync_one_section_VDB(VDB, key, VDB_path, ex_path, rebuild, rebuild_text_db)` | One attribute's index-vs-Excel sync (see _sync_sections_VDB). |
| `_source_uuids_and_json(MF, source)` | ``(recorded uuids, per-uuid json path getter)`` for one analysis source. |
| `rebuild_section_text_db(MF, VDB, key, master_excel_file)` | Rebuild ONE attribute's text DB (its section Excel + JSON, and its column on the master workbook) from the space's analysis JSONs. |
| `rebuild_vector_databases(Storage_path)` | One-time (or on-demand) full rebuild of every section's FAISS index from its Excel DB "Content" column. |
| `_sync_analysis_source_text(MF, VDB, master_excel_file, uuid_cache, source)` | Text-DB sync for one non-abstract analysis source ('intro' or 'rescon'): iterate the source's log, load each document's analysis JSON and write its section keys into their own per-section Excel/JSON DBs + master Excel sheets — exactly how the recorded abstracts are synced. |
| `generate_databases(Storage_path, do_text, do_vector, rebuild_vector, progress_callback)` | Sync the RAG databases for a storage space. |
| `_sections_label(keys)` | Canonical comma-joined section list for the manifest 'Sections' column. |
| `_parse_sections_label(value)` | Inverse of _sections_label. Rows written before the 'Sections' column existed come from abstract-only builds, so they are credited with the abstract sections (never intro/rescon, which no old build ever copied). |
| `_norm_key(value)` | Normalize a title/filename for duplicate comparison. |
| `_usable_title(title)` | — |
| `_usable_filename(filename)` | — |
| `_load_common_known(common_path, sections)` | Load the identity sets of everything already inside the common DB. |
| `_identity_dupe(known, title, filename, match_filename)` | Same publication already in the common DB under ANOTHER UUID? |
| `_success_metadata_map(MF)` | Read the space's success Excel ONCE -> {uuid: (title, filename)}, or None when it can't be read (callers then fall back to per-UUID lookups). |
| `_save_common_manifest(common_path, manifest_rows)` | — |
| `_save_common_skiplog(common_path, skipped_rows)` | Append this run's failures to the common DB's skip log (never overwrites earlier runs). |
| `_sql_documents_for_space(space_path)` | Fetch the analyzed documents of one storage space from the app-wide SQL database (the space is "linked" once sync_storage_to_sql ran for it). |
| `_collect_space_documents(space_path, known, selected_set, match_filename)` | One space's documents for the common-DB merge, prefiltered against what the common DB already holds so that NO per-document work happens for already-merged data: - Candidate UUIDs come from the SQL rows (when the space is linked) plus the abstract / Introduction / Results & Conclusion analysis logs, so a document that only has intro or rescon data is found too. |
| `build_common_database(source_paths, common_path, match_filename, do_vector, section_keys, progress_callback, should_cancel)` | Merge several storage spaces into ONE common text + vector database (per-section Excel/JSON DBs, master Excel overview, FAISS .bin indexes) living in ``common_path`` — the RAG counterpart of the app-wide SQL DB. |
| `generate_combined_databases(Source_path, Storage_path, rebuild_vector)` | — |

### `src/alr/rag_builders/master_excel_db_builder.py`

Builds the combined master Excel workbook, one sheet per analyzed attribute.  
*545 lines · 2 classes · 13 functions*

**class `_WorkbookSession`** — In-memory image of one master workbook + the section JSONs beside it.

| Method | Purpose |
| --- | --- |
| `__init__(self, master_excel_path)` | — |
| `_load(self)` | — |
| `flush(self)` | Write the workbook and every touched JSON DB back to disk. |

**class `workbook_session`** — Context manager batching every :func:`save_to_db` write for one master workbook: read once, apply in memory, write once on exit.

| Method | Purpose |
| --- | --- |
| `__init__(self, master_excel_path)` | — |
| `__enter__(self)` | — |
| `__exit__(self, exc_type, exc, tb)` | — |

| Function | Purpose |
| --- | --- |
| `_save_master_skiplog(master_excel_path, skipped_rows)` | Write the master-workbook skip log next to the workbook itself. |
| `_active_session(master_excel_path)` | — |
| `_apply_section_entry(sheets, order, sheet_name, data_entry)` | Add one entry to its section sheet and mirror it onto the Overview sheet, in memory. |
| `save_to_db(master_excel_path, sheet_name, json_path, data_entry)` | Appends data to a specific sheet inside a single Master Excel file, updates a master 'Overview' sheet at the 1st position with section columns, and appends to a JSON list. |
| `_load_recorded_abstracts(MF)` | Reads UUID column from abstract log; returns [] if unavailable (and prints). |
| `_fetch_metadata(MF, UUID)` | Fetch title and filename from success excel for a given UUID. |
| `_load_abstract_json(MF, UUID)` | Load abstract JSON for the current UUID; returns None on failure (and prints). |
| `_load_json_file(path, UUID, label)` | Load one analysis JSON; returns {} when absent/unreadable (and prints). |
| `_load_analysis_json(MF, UUID, sources)` | Merge the analysis JSONs named in ``sources`` (any of "abstract" / "intro" / "rescon") into one ``{section_key: value}`` dict. |
| `_sync_sections_master_for_uuid(UUID, title, file_name, json_data, sections)` | Iterate sections and save either list items or a single string entry. |
| `_save_list_section(UUID, key, content_list, ex_path, sheet_name, j_path, title, file_name)` | Save list-like sections onto specific sheets as a single bulleted string. |
| `_save_single_section(UUID, key, content_value, ex_path, sheet_name, j_path, title, file_name)` | Save single-string sections onto specific sheets. |
| `build_master_excel_db(storage_path, master_excel_path, progress_callback, should_cancel, section_keys)` | Consolidate the per-section analyzed data of a storage space into a single master Excel workbook (one "Overview" sheet + one sheet per section). |

### `src/alr/rag_builders/query_executor.py`

Runs a RAG query over the section databases and writes the query report workbook.  
*599 lines · 0 classes · 11 functions*

| Function | Purpose |
| --- | --- |
| `batch_enrich_reports(base_storage_path)` | Scans base_storage_path for *_query_Overview_report.xlsx, locates the associated Abstract_Json_files folder, and enriches them. |
| `generate_query_report_RA_KC(query_list, Storage_path, top_k)` | — |
| `generate_query_report(query_list, storage_path, search_root, top_k, section_keys, enrich_keys, harvest_files, progress_callback, resolve_search_root)` | section_keys: optional iterable of section keys to query (any mix of abstract, Introduction and Results & Conclusion attributes — see sections.ALL_RAG_SECTIONS). |
| `process_attribute_query(query, attr, excel_ref, bin_path, vdb, top_k)` | Search one attribute's index and write its result Excel. |
| `_remove_if_empty(folder)` | Delete ``folder`` when it exists and holds nothing. |
| `_enrich_keys_by_source(enrich_keys)` | Group the requested attribute keys by the analysis JSON they come from. |
| `_default_enrich_sections()` | The abstract sections, used as the default attribute set for enrich/log. |
| `_embedding_model_label()` | A human label for the embedding backend used by the vector search, so the query log records which model produced the matches. |
| `_log_query_run(log_path, row)` | Append one row of query details to the rolling query-log Excel, creating it on first use. |
| `enrich_overview_with_abstracts(overview_path, json_folders, enrich_keys, search_root)` | Add one column per selected analyzed attribute to the query overview Excel, reading each document's analysis JSONs by ``Original_UUID``. |
| `harvest_query_resources(overview_path, search_root, vdb, mf, enrich_keys)` | Copy the analysis JSONs behind the matched documents next to the query report and merge the selected attributes into the overview Excel. |

### `src/alr/rag_builders/section_rebuilder.py`

alr.rag_builders.section_rebuilder ================================== Rebuild one or more attributes' section databases from the per-document analysis JSONs: the section Excel/JSON is rewritten from source, then the FAISS index is re-embedded from that Excel.  
*669 lines · 1 classes · 24 functions*

**class `CommonRebuildError` (RuntimeError)** — Raised when an attribute cannot be rebuilt without dropping documents.

| Method | Purpose |
| --- | --- |
| `__init__(self, key, unresolved)` | — |

| Function | Purpose |
| --- | --- |
| `_all_keys()` | Every rebuildable attribute: abstract + Introduction + Results & Conclusion. |
| `_analysis_source_of(key)` | Which analysis JSON an attribute's content comes from. |
| `_load_source_json(MF, uuid, source)` | One document's analysis JSON for the given source ({} when absent). |
| `_recorded_for_source(MF, source)` | The documents a space has recorded for one analysis source, in log order. |
| `unresolved_log_name(when)` | Timestamped filename for one run's unresolved-members log. |
| `find_common_db_config(common_path)` | Return the Path of the common DB's config file, or None. |
| `load_common_db_sources(common_path)` | Read the common DB's config and return ``(sources, match_filename)``. |
| `_placeholder(value)` | Blank/None content becomes the placeholder. |
| `_section_rows(uuid, content_value, title, file_name)` | Build the Excel rows for one document's value of one attribute. |
| `_rows_to_frame(rows)` | Rows -> DataFrame with a stable column order. |
| `_atomic_replace(tmp_path, final_path)` | — |
| `_write_section_excel_atomic(path, frame)` | — |
| `_write_section_json_atomic(path, frame)` | Mirror the Excel exactly -- same rows, same order, same values. |
| `_backup_once(path, suffix)` | Keep one copy of the pre-rebuild file next to the original. |
| `read_section_excel(path)` | Read a section Excel as strings with blanks preserved as empty cells. |
| `read_content_column(path)` | Positional list of Content values, blanks replaced by the placeholder. |
| `_members_from_frame(frame, match_filename)` | Ordered-unique membership keys taken from a section Excel. |
| `read_common_members(common_path, match_filename)` | The common DB's membership, in the order it was built. |
| `_index_sources(sources, match_filename, should_cancel)` | Map every membership key to the ``(source_path, uuid)`` it came from. |
| `_log_unresolved(storage_path, key, unresolved, log_path)` | Write one run's unresolved members to a timestamped file in the space. |
| `_rebuild_index_from_excel(VDB, key, excel_path, bin_path)` | Re-embed one attribute's index from its section Excel, positionally. |
| `rebuild_one_section(storage_path, key, plan, match_filename, should_cancel)` | Rebuild a single attribute: Excel, then JSON, then index. |
| `_build_plan(storage_path, sources, match_filename, should_cancel, source)` | Decide which documents to emit, in which order. |
| `rebuild_section_databases(storage_path, keys, sources, match_filename, should_cancel, progress_callback)` | Rebuild the chosen attributes' section Excel/JSON and FAISS index. |

### `src/alr/rag_builders/text_db_updater.py`

Appends analyzed attribute values into the per-section text databases (Excel + JSON).  
*306 lines · 0 classes · 9 functions*

| Function | Purpose |
| --- | --- |
| `_load_db_pair(excel_path, json_path)` | Read an Excel/JSON DB pair ONCE and return a cache entry for save_to_db: {"excel_df": DataFrame or None, "excel": set of UUID strings, "json_data": list, "json": set of UUID strings, "excel_read_error": bool} Used by save_to_db's uuid_cache fast path so one sync run performs one read per file instead of one read per entry. |
| `flush_db_cache(uuid_cache)` | Write every pending change held in ``uuid_cache`` to disk. |
| `save_to_db(excel_path, json_path, data_entry, uuid_cache)` | Appends data to an Excel file and a JSON list with explicit engine specification. |
| `_load_recorded_abstracts(MF)` | Reads UUID column from abstract log; returns [] if unavailable (and prints). |
| `_fetch_metadata(MF, UUID)` | Fetch title and filename from success excel for a given UUID. |
| `_load_abstract_json(MF, UUID)` | Load abstract JSON for the current UUID; returns None on failure (and prints). |
| `_sync_sections_for_uuid(UUID, title, file_name, json_data, sections, uuid_cache)` | Iterate sections and save either list items or a single string entry. |
| `_save_list_section(UUID, key, content_list, ex_path, j_path, title, file_name, uuid_cache)` | Save list-like sections (Research Areas, Key Concepts, etc.) exactly as before. |
| `_save_single_section(UUID, key, content_value, ex_path, j_path, title, file_name, uuid_cache)` | Save single-string sections exactly as before. |

### `src/alr/rag_builders/vector_db_updater.py`

Embeds strings and maintains the per-section FAISS indexes.  
*426 lines · 0 classes · 14 functions*

| Function | Purpose |
| --- | --- |
| `_resolve_default_embedding_method()` | — |
| `_current_backend()` | Resolve the effective (method, service) at call time. |
| `_record_last_backend(method, service, model, dim)` | — |
| `_meta_path(index_file)` | — |
| `_recorded_model(method, service, model)` | Best-effort identifier of the embedding model an index was built with. |
| `write_index_metadata(index_file, method, service, model, dim)` | — |
| `read_index_metadata(index_file)` | — |
| `_validate_query_against_index(index_file, method, service, model, query_dim)` | Warn/raise when the query embedding backend differs from what built the index. |
| `vectorize_strings(input_strings, method, service, model, max_length, batch_size, max_retries, retry_wait)` | Get embedding vectors for a list of strings. |
| `create_faiss_index_cosine(vectors)` | — |
| `save_index_file(index, file_path, method, service, model)` | — |
| `load_index_file(file_path)` | — |
| `add_new_strings_to_index(index_file, new_strings, method, service, model, max_length)` | — |
| `search_similar(index_file, query, top_k, method, service, model, max_length)` | — |

## Evaluation layer

*Scoring how well the analyzed data is supported by the source text.* — `alr/analysis_evaluation/`

### `src/alr/analysis_evaluation/Distance_w_Structural _Alignment.py`

Levenshtein distance/ratio and word error rate (note: the filename contains a space).  
*53 lines · 0 classes · 1 functions*

| Function | Purpose |
| --- | --- |
| `calculate_edit_distance_metrics(text1, text2)` | Calculates Character-level Levenshtein Distance and Word Error Rate (WER). |

### `src/alr/analysis_evaluation/Lexical_Overlap_Metrics.py`

Jaccard, ROUGE-1/2/L and BLEU between a reference and a candidate string.  
*79 lines · 0 classes · 3 functions*

| Function | Purpose |
| --- | --- |
| `calculate_jaccard_similarity(text1, text2)` | Calculates Jaccard Similarity based on unique words (tokens). |
| `calculate_rouge_scores(reference, candidate)` | Calculates ROUGE-1, ROUGE-2, and ROUGE-L F1-scores. |
| `calculate_bleu_score(reference, candidate)` | Calculates the BLEU score using NLTK. |

### `src/alr/analysis_evaluation/data_evaluator.py`

Grounding evaluation: how much of each extracted attribute is supported by the source text.  
*769 lines · 0 classes · 26 functions*

| Function | Purpose |
| --- | --- |
| `_normalize_for_match(text)` | Lowercase and strip all whitespace so line breaks/spacing don't break the match. |
| `_is_subset_match(content_str, abs_txt)` | Whitespace-insensitive, case-insensitive substring containment check. |
| `_is_placeholder(text)` | True when a value is one of the pipeline's "nothing here" markers. |
| `_usable_reference(text)` | True when a reference text is real content rather than a missing marker. |
| `_grade(key, content_str, reference, vocabulary)` | Grounding of one extracted value against the reference text. |
| `safe_sheet_title(name)` | Excel worksheet titles are capped at 31 characters and cannot contain ``[ ] : * ? / \``. |
| `_is_duplicate_in_sheet(file_path, sheet_name, target_uuid)` | Checks if a UUID already exists in a given Excel sheet. |
| `_apply_flat_entry(book, sheet_name, data_entry)` | Insert-or-update one UUID row on ``sheet_name`` of an in-memory book. |
| `_write_section_sheet_flat(file_path, sheet_name, data_entry)` | Appends or updates a data entry for section files ensuring single row flat format per UUID. |
| `_update_master_overview(storage_dir, sheet_name, uuid, title, filename, text_content, true_count, false_count, full_section_entry, overview_path)` | Updates the centralized Master Overview tracking true/false count per section and bulleted texts. |
| `_sync_sections_for_uuid(UUID, title, file_name, json_data, sections, storage_path, text_key, overview_path, overwrite, rows_out)` | Iterates through layout mappings to transform and evaluate section lists or strings. |
| `_grounding_row(key, uuid, title, file_name, item_no, content, columns)` | One row of the per-metric workbook's ``grounding`` sheet. |
| `_save_list_section(UUID, key, content_list, ex_path, title, file_name, abs_txt, storage_path, overview_path, overwrite, vocabulary, rows_out)` | Flattens lists horizontally using alternation layouts and uploads aggregate statistics. |
| `_save_single_section(UUID, key, content_value, ex_path, title, file_name, abs_txt, storage_path, overview_path, overwrite, vocabulary, rows_out)` | Saves single text objects cleanly mapping their matching criteria directly. |
| `_eval_score(stats)` | Overall grounding from per-section stats: (percent, total_true, total_false). |
| `_push_eval_to_sql(uuid, stats, db_path, target)` | Write the evaluation summary (per-section subset counts + overall score) into the SQLite document row, if that row exists. |
| `_existing_evaluation(uuid, db_path, target)` | Return the stored per-section stats for ``uuid`` if this document has already been evaluated for ``target`` (its SQL score column is present), else ``None``. |
| `_load_intro_json(MF, uuid)` | Load {uuid}_Intro.json from the space's Introduction data folder (or None). |
| `_load_recorded_intros(MF)` | UUIDs with a recorded introduction analysis (mirrors _load_recorded_abstracts). |
| `_load_rescon_json(MF, uuid)` | Load {uuid}_Results_Conclusion.json from the space's Results & Conclusion folder (or None). |
| `_load_recorded_rescons(MF)` | UUIDs with a recorded Results & Conclusion analysis. |
| `evaluate_document(storage_path, uuid, db_path, push_sql, mode, target, per_metric)` | Evaluate one analyzed document (by ``uuid``): write its per-section evaluation Excel sheets and, when ``push_sql`` is set, mirror the summary into the SQLite store so overviews can use it. |
| `_eval_workbooks(MF, target)` | ``(overview_path, [per-section eval workbook paths])`` for one target. |
| `evaluate_space(storage_path, db_path, progress_callback, should_cancel, mode, target, per_metric)` | Evaluate every analyzed document in a storage space (build the evaluation Excel DBs and sync the summary into SQLite). |
| `generate_databases(Storage_path)` | Back-compatible entry point: evaluate the whole space (Excel DBs + SQL sync). |
| `generate_combined_databases(Source_path, Storage_path)` | — |

### `src/alr/analysis_evaluation/metric_evaluator.py`

alr.analysis_evaluation.metric_evaluator ======================================== Batch metric evaluation over a storage space -- the batch counterpart of the manual two-text comparison panel, following the same space-wide pattern as :mod:`data_evaluator` (which handles the substring/grounding check).  
*956 lines · 1 classes · 18 functions*

**class `_CosineContext`** — Embedding cosine similarity against a per-document reference vector, reusing the storage space's per-section FAISS indexes where possible (same functions and index/metadata handling as query_executor).

| Method | Purpose |
| --- | --- |
| `__init__(self, vdb, section_keys)` | — |
| `_normalize(vec)` | — |
| `embed(self, text)` | — |
| `embed_many(self, texts)` | Embed several texts in ONE call; returns a (n, dim) row-normalized matrix. |
| `current_model()` | Identity of the embedding model that produced the most recent vectors (set by vectorize_strings, fallbacks included). |
| `_index_conflict(bin_path, index)` | Why an existing index must NOT be reused with the current embedding backend, or None when it is safe. |
| `_section_index(self, section_key)` | Load (index, contents) for a section; create the index if missing. |
| `item_vector(self, section_key, item_text)` | Reconstruct the item's stored vector from the section index, else embed it. |
| `sentence_similarities(self, section_key, item_text, sent_mat)` | Cosine of one item against EVERY reference sentence (one value per sentence, in order). |

| Function | Purpose |
| --- | --- |
| `_split_sentences(text)` | Split a reference text into individual sentences (regex-based on ./!/? boundaries — deliberately not nltk, whose punkt data may be unavailable). |
| `_best_value(col, values)` | Best non-None value of one metric column across the sentences (min for distance-like columns, max otherwise). |
| `_ensure_punkt()` | BLEU needs nltk punkt data; try a one-time quiet download if missing. |
| `_lexical_metrics(reference, candidate)` | Jaccard/ROUGE/BLEU; each guarded so one failure records None, not an abort. |
| `_distance_metrics(reference, candidate)` | Levenshtein + WER from the structural-alignment module (space in filename). |
| `_target_config(MF, VDB, target)` | Section keys, JSON loader, reference-text key, workbooks and UUID source per target. |
| `_sentence_metric_rows(kinds, cosine_ctx, section_key, sentences, candidate, sent_mat)` | Compute the selected metric columns for one extracted item against EVERY reference sentence. |
| `_add_per_metric_rows(book, kinds, uuid, section_key, rows)` | Fan one section's per-item rows out onto the per-metric workbook: each metric column becomes its own sheet, carrying the value, the reference sentence that produced it and the section it came from. |
| `_write_section_rows(file_path, sheet_name, uuid, rows)` | Write a document's rows on a section sheet: ONE ROW PER EXTRACTED ITEM (unlike data_evaluator's _write_section_sheet_flat, which keeps one row per document). |
| `_sentence_detail_path(details_dir, uuid, sql_label, max_length)` | Build the per-document detail JSON path, kept under a safe total length so a deeply nested storage tree (long space folder + section subfolders) or a long filename-derived ``uuid`` doesn't push the path past the OS limit and silently drop the file. |
| `_write_sentence_detail_json(details_dir, uuid, sql_label, payload)` | Write the per-document sentence-level metric detail file (``{uuid}_{target}_Sentence_Metrics.json``). |
| `_push_metrics_to_sql(uuid, averages, sql_label, db_path)` | Merge per-document metric averages into the metrics_json column (if row exists). |
| `_previous_dated_workbook(current_path)` | Latest sibling workbook with the same name but an EARLIER date prefix. |
| `_rows_from_detail_json(details_dir, uuid, sql_label, needed_cols)` | Rebuild the new-format item rows ({section: [row, ...]}) from the per-document sentence-detail JSON, references included (older detail files carry only the sentence number - the text is recovered from the stored "Reference sentences" list). |
| `_rows_from_previous_workbook(current_overview_path, uuid, needed_cols)` | Rebuild the item rows for ``uuid`` from the latest PREVIOUS dated combined-overview workbook. |
| `_reuse_prior_metrics(cfg, kinds, uuid, db_path, per_metric)` | Copy mode: if previously recorded metric data exists for ``uuid`` - the sentence-detail JSON first, else the latest previous dated workbook (old or new format) - re-write it into TODAY's workbooks in the new one-row-per-item format (per-kind + combined + Overview averages), push the averages to SQL, and return True. |
| `_existing_metrics(uuid, metric_cols, sql_label, db_path)` | Return the stored per-document metric averages for ``uuid`` if its ``metrics_json[sql_label]`` already covers every selected metric column, else ``None``. |
| `evaluate_space_metrics(storage_path, kinds, target, db_path, progress_callback, should_cancel, mode, per_metric)` | Batch-compute the selected metric ``kinds`` (subset of :data:`METRIC_KINDS`) for every analyzed document in a storage space, against the ``target`` data ("abstract", "intro" or "rescon"). |

### `src/alr/analysis_evaluation/per_metric_sheets.py`

alr.analysis_evaluation.per_metric_sheets ========================================= One workbook per target holding **one sheet per individual metric** — the adaptation of the data-extraction repo's ``column_evaluator.write_metric_sheets`` to this repo's data.  
*171 lines · 1 classes · 3 functions*

**class `PerMetricWorkbook`** — Buffered writer for one target's per-metric workbook.

| Method | Purpose |
| --- | --- |
| `__init__(self, path, enabled)` | — |
| `add(self, sheet, uuid, rows)` | Buffer ``rows`` as part of document ``uuid``'s content of ``sheet``. |
| `flush(self)` | Write every buffered sheet in ONE workbook rewrite and clear the buffer. |
| `close(self)` | Final flush; returns the workbook path if anything was written. |
| `__enter__(self)` | — |
| `__exit__(self, *exc)` | — |

| Function | Purpose |
| --- | --- |
| `_ordered(sheets)` | Known sheets in canonical order, then anything unexpected, sorted. |
| `_row_columns(rows)` | Union of the rows' keys, base columns first, then first-seen order. |
| `open_for(vdb, target, enabled)` | The per-metric workbook of one target, from the workbook registry. |

### `src/alr/analysis_evaluation/publication_classification/Classification_logic_with_Q's.py`

Question-scored classification: asks a fixed question set per document and scores the answers.  
*218 lines · 0 classes · 3 functions*

| Function | Purpose |
| --- | --- |
| `Classification_of_Phrase(title, question, max_retries)` | — |
| `save_all_sheets(summary_df, sheets_dict, output_file_path)` | Saves the Main Summary sheet followed by all individual component section sheets. |
| `classify_excel_data_to_sheets(file_path, column_name, output_file_path, progress_callback)` | Evaluates each title in an Excel column against all classification sections. |

### `src/alr/analysis_evaluation/publication_classification/classification_questions.py`

The question bank and prompt builders used by question-scored classification.  
*244 lines · 0 classes · 0 functions*

### `src/alr/analysis_evaluation/publication_classification/classify_runner.py`

alr.analysis_evaluation.publication_classification.classify_runner ================================================================= Bridge the standalone publication-classification logic into the analysis flow.  
*385 lines · 0 classes · 12 functions*

| Function | Purpose |
| --- | --- |
| `_load_existing_classification(excel_path)` | If ``excel_path`` already exists and has rows, return ``(existing_rows, existing_filenames)`` -- a list-of-dict copy of the current rows (for re-saving/merging) and the set of filenames already present. |
| `has_existing_classification(manager, kind)` | Return how many documents already have classification data saved in the managed workbook -- ``Publication_Classification.xlsx`` for ``kind="title"``, or ``Abstract_Classification.xlsx`` for ``kind="abstract"``. |
| `_kind_config(manager, kind)` | — |
| `_text_is_valid(kind, text)` | — |
| `_append_classification_row(excel_path, row)` | Merge one classification row into the (dated) workbook, keyed by ``filename``: replace an existing row for that file, otherwise append. |
| `copy_classification_from_previous(manager, filename, title, kind, db_path, uuid)` | Reuse a prior dated classification result for one document instead of calling the LLM. |
| `classify_document(manager, doc, kind, db_path, service, mode, store)` | Classify a single document (``doc`` is a SQLite row dict with ``uuid`` / ``filename`` / ``title`` / ``abstract_text``) and persist the result. |
| `_run_classification(manager, kind, db_path, progress_callback, should_cancel, overwrite, service, mode)` | — |
| `classify_space(manager, db_path, progress_callback, should_cancel, overwrite, service, mode)` | Classify each document in a storage space by title and persist the result (SQLite ``classification`` column + managed dated ``Title_Classification.xlsx``). |
| `classify_abstract_space(manager, db_path, progress_callback, should_cancel, overwrite, service, mode)` | Classify each document by its **identified abstract text** and persist the result (SQLite ``abstract_classification`` column + managed dated ``Abstract_Classification.xlsx``). |
| `classify_custom_space(manager, topic, tags, source, db_path, progress_callback, should_cancel, service)` | Classify every analyzed document in a storage space against a **user-defined** tag taxonomy (custom classification prompt built from ``tags``; see ``title_classifier.build_custom_classifier_prompt``). |
| `question_score_space(manager, source, download_log, output_excel, progress_callback)` | Run the question-scored publication classification for a storage space. |

### `src/alr/analysis_evaluation/publication_classification/title_classifier.py`

Classifies a publication from its title (and the custom-topic prompt builder).  
*354 lines · 0 classes · 6 functions*

| Function | Purpose |
| --- | --- |
| `classify_title(title, service)` | Classify a publication by its title against the taxonomy. |
| `build_custom_classifier_prompt(topic, tags)` | Build a classification system prompt for a user-defined taxonomy. |
| `_tag_key(tag)` | Normalised comparison key for a tag name (case/whitespace-insensitive). |
| `_coerce_classifier_bool(val)` | Turn an LLM-returned decision into a real boolean. |
| `classify_custom(text, tags, topic, service, source_label)` | Classify a publication text against a **user-defined** tag taxonomy. |
| `classify_abstract(abstract_text, service)` | Classify a publication against the same taxonomy as :func:`classify_title`, but using the identified abstract text (from the abstract analyzer) instead of the title. |

### `src/alr/analysis_evaluation/word_grounding.py`

alr.analysis_evaluation.word_grounding ====================================== Word-level grounding for the substring/grounding evaluation.  
*146 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `tokenize(text)` | Every word of ``text``, lower-cased, in order (stopwords included). |
| `content_words(text)` | The words of ``text`` that carry content: :func:`tokenize` minus :data:`STOPWORDS`, in their original order, **duplicates kept**. |
| `reference_words(reference)` | The set of whole words present in a reference text, for lookup. |
| `grounding(value, reference, vocabulary)` | Word-level grounding of one extracted ``value`` against ``reference``. |
| `missing_preview(result, limit)` | The ungrounded words of a :func:`grounding` result as one cell value. |

## Shared services

*Storage layout, Excel/JSON/SQL access, LLM plumbing, logging and cleanup.* — `alr/common/`

### `src/alr/common/LLM_Config.py`

Provider/model registry, API-key storage and the embedding-backend selection.  
*155 lines · 0 classes · 7 functions*

| Function | Purpose |
| --- | --- |
| `_load_config()` | Return the persisted API-key config as a dict (empty if none/invalid). |
| `_load_persisted_keys_into_env()` | On import, copy any persisted keys into os.environ (unless already set in the environment), so stored keys behave like env vars across launches. |
| `get_stored_api_key(API_Key_type)` | Return the API key for a service from the environment (or persisted config), or None if it has not been set. |
| `set_api_key(API_Key_type, api_key)` | Store an API key: set it in os.environ for the current session AND persist it to the config file (merging, not overwriting other keys) so it is loaded on the next launch. |
| `delete_api_key(API_Key_type)` | Remove a stored API key from the environment and the config file. |
| `get_api_key(API_Key_type)` | CLI helper: return the stored key, or prompt for it on the console and persist it. |
| `check_api_key(API_Key_type)` | Return the stored key, prompting on the console (CLI) only if missing. |

### `src/alr/common/System_prompts.py`

Prompts shared across more than one pipeline stage.  
*42 lines · 0 classes · 0 functions*

### `src/alr/common/activity_monitor.py`

alr.common.activity_monitor =========================== A tiny, thread-safe registry of **live model activity** so the desktop UI can always show what the backend is doing: which chat/embedding model is being called right now, how many tokens have been processed by text-generation models, and how many vectors have been embedded.  
*147 lines · 1 classes · 11 functions*

**class `active`** — Context manager: ``with active('embed', service, model): ...``.

| Method | Purpose |
| --- | --- |
| `__init__(self, kind, service, model)` | — |
| `__enter__(self)` | — |
| `__exit__(self, *exc)` | — |

| Function | Purpose |
| --- | --- |
| `_blank_chat()` | — |
| `_blank_embed()` | — |
| `_coerce_int(v)` | — |
| `_log_path()` | — |
| `_append_log(line)` | — |
| `begin(kind, service, model)` | Mark a call as running (``kind`` is 'chat' or 'embed'). |
| `end()` | Clear the in-flight marker. |
| `record_chat(service, model, in_tokens, out_tokens, time_taken)` | — |
| `record_embedding(service, model, n_vectors, vector_dim, time_taken)` | — |
| `snapshot()` | A copy of the current activity state (safe to read from the UI thread). |
| `reset()` | Zero the counters (e.g. the user cleared the session). |

### `src/alr/common/analysis_precheck.py`

alr.common.analysis_precheck ============================ Single source of truth for "does this document already have X, and where".  
*357 lines · 0 classes · 12 functions*

| Function | Purpose |
| --- | --- |
| `_nonempty(value)` | True if a SQL/cell value carries real content (not None/blank/'nan'). |
| `_date_key(path)` | Sort key: (YYYY-MM-DD from the filename or '', mtime) -- newest first later. |
| `find_dated_files_with(folder, name_contains, key_col, key_value, sheet_name)` | Return the dated ``.xlsx`` workbooks under ``folder`` whose name contains ``name_contains`` and that hold a row where ``key_col`` equals ``key_value``. |
| `latest_dated_row(folder, name_contains, key_col, key_value, sheet_name)` | Return ``(path, row_dict)`` for the newest workbook under ``folder`` that contains ``key_value`` in ``key_col`` -- the most recent prior result to copy from -- or ``(None, None)`` when nothing matches. |
| `_json_present(path)` | True if a JSON file exists and is non-empty. |
| `_abstract_json_ok(abstract_path)` | True if the abstract JSON exists and carries no 'no abstract' error token. |
| `_eval_storage_present(MF, uuid)` | True if a dated evaluation overview already records this UUID on disk. |
| `document_status(MF, uuid, filename, doc_row)` | Report, for one document, whether each analysis / enrichment artifact is present in the on-disk **storage space** and in the **SQL** row. |
| `_eval_overview_folders(MF)` | (folder, name_contains) per evaluation target, or {} if unavailable. |
| `compute_space_gaps(MF, db_path)` | Report, for a whole storage space, which enrichment stages are **missing from SQL** and which of those could be filled by data that already exists on disk (a prior dated workbook, or an analysis JSON that simply hasn't been synced). |
| `_eval_reusable(eval_folders, stage, uuid)` | True if a dated evaluation overview for ``stage`` already records ``uuid``. |
| `locate_document_data(MF, uuid, filename)` | List, per enrichment kind, the dated workbooks in the storage space that hold this document's data. |

### `src/alr/common/artifact_cleanup.py`

alr.common.artifact_cleanup =========================== The :class:`~alr.common.file_manager.DataAnalyzeManager` eagerly creates the full storage-space folder tree (and per-document sub-folders) up front, so after an analysis run a storage space often contains folders and files that were never actually written to — e.g.  
*147 lines · 0 classes · 5 functions*

| Function | Purpose |
| --- | --- |
| `_is_empty_file(path)` | True if ``path`` is a regular file with no meaningful content. |
| `prune_empty_artifacts(root, should_cancel)` | Remove empty files and empty directories under ``root`` (bottom-up), leaving ``root`` itself in place even if it ends up empty. |
| `_top_level_only(folders)` | Drop any folder that already sits inside another one in the list -- pruning the parent walks the child anyway, so keeping both would re-walk the tree. |
| `_is_within(path, parent)` | True if ``path`` is ``parent`` or lives underneath it. |
| `prune_touched_folders(should_cancel)` | Prune every managed folder tree opened since the last call. |

### `src/alr/common/crash_logger.py`

Automatic crash logging for the Automated Literature Review apps.  
*145 lines · 0 classes · 4 functions*

| Function | Purpose |
| --- | --- |
| `write_crash_log(exc_type, exc_value, exc_tb, origin)` | Write one crash-report file with the full traceback; return its path. |
| `_notify_console(message)` | Echo to the real terminal, bypassing the in-app stdout redirection. |
| `install(app_name)` | Install process-wide crash hooks (main thread + background threads). |
| `attach_to_tk(root, app_name)` | Route Tk callback exceptions on ``root`` into the crash log. |

### `src/alr/common/document_inspector.py`

document_inspector.py - SQL-first single-document lookup for the Review tool.  
*352 lines · 0 classes · 13 functions*

| Function | Purpose |
| --- | --- |
| `_load_json(path)` | Return (parsed, raw_text) for a JSON file. |
| `_blank(value)` | — |
| `lookup_sql_documents(store, mode, term)` | Find documents in the SQL database by UUID, Title or Filename. |
| `is_storage_space(folder)` | A folder counts as a storage space when it holds the processed-file registry. |
| `find_registry_documents(space_folder, mode, term)` | Find documents in a storage space's Processed_file_registry.xlsx by UUID, Title or Filename (same matching rules as the SQL lookup). |
| `load_space_payloads(space_folder, uuid)` | Read the document's on-disk analysis JSONs from a storage space: abstract, introduction, results & conclusion and references. |
| `_as_text(value)` | — |
| `assemble_document_view(sql_row, registry_row, payloads)` | Build the full picture of one document as ordered rows ``(group, field, value, source)``. |
| `missing_after_merge(view_rows)` | Groups that ended up with no data at all (helps the UI hint at gaps). |
| `document_filename(sql_row, registry_row)` | — |
| `candidate_pdf_paths(sql_row, registry_row, space_folder, payloads)` | Known places the PDF could be, in preference order: the recorded relative_path, then ``pdf_files/<filename>`` under the SQL source_folder, the chosen space and the payloads' pdf_dir. |
| `find_pdf(sql_row, registry_row, space_folder, payloads)` | First existing file among the known candidate paths, or None. |
| `search_pdf_recursive(root, filename, should_cancel, progress_callback)` | Search ``root`` and every nested subfolder for files named ``filename`` (case-insensitive). |

### `src/alr/common/download_log_enrich.py`

alr.common.download_log_enrich ============================== Identify bibliographic metadata for analyzed documents by scanning **all** ``*_download_log.xlsx`` workbooks under a root folder and fuzzy-matching each document's title against the log's ``Publication Name`` column (or, failing that, its filename against a file-name column).  
*146 lines · 0 classes · 4 functions*

| Function | Purpose |
| --- | --- |
| `_norm(text)` | — |
| `_find_col(columns, *candidates)` | Return the first column whose normalized name matches any candidate. |
| `_collect_log_rows(logs)` | Read every download-log workbook into a single list of normalized row dicts. |
| `enrich_from_download_logs(root, db_path, threshold, progress_callback, should_cancel)` | Scan all ``*_download_log.xlsx`` files under ``root`` and enrich documents in the SQLite store by fuzzy-matching their title against each log's Publication Name (falling back to a filename match). |

### `src/alr/common/excel_session.py`

alr.common.excel_session ======================== Batched read/modify/write for the multi-sheet Excel workbooks this app keeps updating one row at a time (the master DB workbook, the evaluation overviews).  
*138 lines · 2 classes · 3 functions*

**class `Book`** — In-memory image of one workbook: ``sheets`` {name: DataFrame} + order.

| Method | Purpose |
| --- | --- |
| `__init__(self, path, first_sheet)` | — |
| `_load(self)` | — |
| `sheet(self, name, columns)` | The named sheet, created empty (with ``columns``) if it is new. |
| `set_sheet(self, name, df)` | — |
| `flush(self)` | Write the workbook out if anything changed. |

**class `workbook_session`** — Context manager batching every write to one workbook (see module doc).

| Method | Purpose |
| --- | --- |
| `__init__(self, path, first_sheet)` | — |
| `__enter__(self)` | — |
| `__exit__(self, exc_type, exc, tb)` | — |

| Function | Purpose |
| --- | --- |
| `active(path)` | The open session's Book for ``path``, or None. |
| `acquire(path, first_sheet)` | ``(book, owned)`` for a writer: the active session's book (owned=False) or a freshly read one (owned=True) that :func:`release` writes out at once. |
| `release(book, owned)` | Write a book acquired with ``owned=True``. |

### `src/alr/common/excel_utils.py`

Cached Excel reads/writes, deferred write sessions, cell-safe updates and checkpointing.  
*592 lines · 1 classes · 17 functions*

**class `Checkpointer`** — Decides when a batch pass should write its buffers to disk.

| Method | Purpose |
| --- | --- |
| `__init__(self, every, min_seconds)` | — |
| `due(self, i)` | — |

| Function | Purpose |
| --- | --- |
| `_stamp(path)` | — |
| `read_excel_cached(path, sheet_name, **kwargs)` | Read a workbook, reusing the last parse while the file is unchanged. |
| `write_excel_cached(df, path, sheet_name, **kwargs)` | Write a workbook and keep the cache in step with what is now on disk. |
| `invalidate_excel_cache(path)` | Drop cached parses (all of them, or just one file). |
| `_flush_locked(paths)` | Write dirty deferred workbooks (all of them, or just ``paths``). |
| `set_cell(df, mask, col, value)` | Assign ``value`` into ``df.loc[mask, col]``, widening the column when the value does not fit its dtype. |
| `checkpoint()` | Flush the deferred workbooks now (call between documents). |
| `workbook_session(*paths)` | Defer writes to ``paths`` for the duration of the block. |
| `get_column_value(excel_file, column_name, idx)` | Retrieve the value from a specific column at a given index in the Excel file. |
| `extract_column(file_path, column_name)` | Extracts all data from a specified column in a single-sheet Excel file into a list. |
| `get_corresponding_value(excel_file_path, column_1, value_1, column_2)` | — |
| `update_corresponding_value(excel_file_path, column_1, value_1, column_2, new_value)` | — |
| `get_values_from_sorted_numbers(excel_file_path, num_column, value_column, n)` | — |
| `get_values_from_sorted_numbers_and_save(excel_file_path, num_column, value_column, n, output_file_path)` | — |
| `add_column_sum(excel_file_path, col1, col2, col3)` | This function loads an Excel file, performs the sum of col1 and col2, and stores the result in col3. |
| `sum_columns_ending_with_to_target(excel_path, suffix)` | For all sheets in the given Excel file: - Find all columns whose names end with `suffix` - Sum them row-wise - Store the sums into a new column 'TOTAL{suffix}' (if it doesn't exist) - Save result back to the same Excel file (overwrites existing file) Notes: - Non-numeric values are treated as 0 (coerced to NaN then filled with 0). |
| `aggregate_query_excel_data(folder_path, column_name, output_file)` | — |

### `src/alr/common/file_handlers.py`

Path safety (length-guarded names), JSON indexing under a search root, file moves.  
*173 lines · 0 classes · 7 functions*

| Function | Purpose |
| --- | --- |
| `move_matching_pdfs(filenames_to_find, search_root, destination_folder)` | Recursively searches for PDFs matching the filenames and moves them to the destination_folder. |
| `safe_path(file_path, max_length)` | Return a filesystem path guaranteed to be a real, writable FILE whose total length stays within ``max_length`` — so a long name (a filename-derived id, a title) or a deeply nested storage tree can't push the path past the OS limit and make the write fail. |
| `sanitize_path_length(file_path, max_length)` | Backwards-compatible wrapper over :func:`safe_path` that returns a ``str``. |
| `copy_matching_jsons(filenames, search_root, dest_folder)` | — |
| `copy_matching_pdfs(filenames, search_root, dest_folder)` | — |
| `copy_file(src, dest_dir)` | Standardized simple copy function. |
| `index_jsons_under_root(wanted_names, search_root)` | One recursive pass over ``search_root`` mapping each wanted JSON filename (``<uuid><suffix>.json``) to its path. |

### `src/alr/common/file_manager.py`

The storage-space layout: every folder and file path the pipeline reads or writes.  
*621 lines · 3 classes · 2 functions*

**class `CollectionManager`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, Collection_Folder)` | Initializes the folder structure and static paths. |
| `ensure_folders(self)` | (Re-)create the managed folder tree and return self. |
| `update_topic_files(self, doc_id)` | Updates the specific JSON paths for a given document ID. |
| `update_llm_service(self, Value)` | — |
| `update_Research_Area(self, Value)` | — |
| `update_Research_Question(self, Value)` | — |
| `update_Research_Scope(self, Value)` | — |
| `update_Keyword_list(self, Value)` | — |
| `update_Keyword_provenance(self, suggested, user_added)` | Record the full keyword universe behind the current selection so the keywords JSON can log all LLM-suggested keywords and any user-added keywords alongside the selected subset (``Keyword_list``). |
| `update_Search_phrase_list(self, Value)` | — |

**class `DataAnalyzeManager`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, folder_path)` | Initializes the folder structure and static paths. |
| `update_id_files(self, doc_id)` | Updates the specific JSON paths for a given document ID. |
| `update_llm_service(self, Value)` | — |
| `describe_folder(path)` | Read-only inspection of a folder to decide whether it is a DataAnalyzeManager storage space, and how complete it is. |

**class `Vec_DB_Manager`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, folder_path)` | Initializes the folder structure and static paths. |
| `update_key_folder(self, key)` | — |
| `update_query_folder(self, query_name)` | — |

| Function | Purpose |
| --- | --- |
| `register_managed_folder(folder)` | Record a folder tree that a manager just created (or re-opened). |
| `take_managed_folders()` | Drain and return the folder roots recorded since the last call. |

### `src/alr/common/general_utils.py`

Small shared helpers: timing, set differences, text normalization, folder cleaning.  
*386 lines · 0 classes · 18 functions*

| Function | Purpose |
| --- | --- |
| `generate_unique_id(filename, existing_ids)` | Generate hash-based ID, append suffix if collision detected. |
| `clean_folder_path(raw_folder_path)` | — |
| `add_hh_mm_ss(time_str1, time_str2)` | — |
| `_as_path(p)` | Convert str/pathlike/Path into Path safely. |
| `caluculate_time_taken(start_time, end_time)` | — |
| `print_two_column_table(items, Header, col_width)` | Prints index--string pairs in two columns with table borders. |
| `Proccess_string_to_list(api_response)` | Cleans the raw AI string output and converts it into a Python list of unique, non-empty keywords. |
| `is_similar(str1, str2, threshold)` | Check if two strings are similar based on a given similarity threshold using word occurrence. |
| `is_similar_Length(str1, str2, threshold)` | Check if two strings are similar based on a given similarity threshold. |
| `merge_lists(initial_list, new_list)` | Merge two lists ensuring no duplicates or near-duplicates (strings matching 80% or more). |
| `find_in_first_n_chars(strings, target, n)` | Returns a list of strings where `target` appears within the first `n` characters. |
| `find_first_match_in_first_n_chars_old(strings, target, n)` | Returns the first string where `target` appears within the first `n` characters. |
| `find_first_match_in_first_n_chars(strings, target, n)` | Returns a tuple of (prev_string, matched_string, next_string) for the first string where `target` appears within the first `n` characters (or anywhere in the string as per the original fallback logic). |
| `find_missing_elements(list_1, list_2)` | — |
| `remove_string_from_list(strings, target)` | — |
| `clean_response_json_text(text)` | — |
| `print_with_separator(input_string, separator, terminal_width, default_width)` | Safe in Jupyter, terminals, and PyInstaller: - avoids os.get_terminal_size() ioctl errors - falls back to default_width when width can't be detected |
| `retry_after_seconds(value)` | Seconds to wait from a Retry-After header, which is either a number of seconds or an HTTP date. |

### `src/alr/common/json_utils.py`

Reading and writing the per-document analysis and section JSON files.  
*293 lines · 0 classes · 7 functions*

| Function | Purpose |
| --- | --- |
| `print_json_file(file_path)` | Reads a JSON file from the given path and prints its content formatted with nice indentation in the terminal. |
| `store_to_json(input_data, file_path, time_taken)` | — |
| `store_to_json_with_text(input_data, file_path, time_taken, text, type)` | — |
| `pretty_print_json_from_file(file_path)` | — |
| `get_key_from_file(file_path, target_key)` | Loads a JSON file and finds all occurrences of target_key. |
| `get_value_by_pair(file_path, anchor_key, anchor_value, target_key)` | Finds a dictionary where anchor_key matches anchor_value, then returns the value of target_key from that same dictionary. |
| `get_chunks_from_references(file_path)` | This function reads the JSON file, searches for sections with various reference headings, and returns the chunks (texts) from the found section. |

### `src/alr/common/llm_utils.py`

All LLM and embedding calls: providers, timeouts, bounded retries, rate limiting, logging.  
*1593 lines · 0 classes · 46 functions*

| Function | Purpose |
| --- | --- |
| `_respect_rate_limit()` | Block until a request slot is free, then claim it (thread-safe). |
| `_post_with_retries(url, headers, payload, timeout, service, max_retries)` | POST with a native requests timeout and retry-with-backoff on transient failures (429/5xx, connection errors, timeouts). |
| `_record_call_info(**kw)` | — |
| `get_last_call_info()` | Return details of the most recent llm_call()/embedding_call(): requested_service, service_used, model_used, fallback_used, error. |
| `get_selected_model(service)` | Return the currently selected model for a service ('BlaBla', 'Chat AI' or 'DLR Ollama'). |
| `set_selected_model(service, model)` | Set the model to use for a service for the rest of the session. |
| `list_blablador_models(blablador_key)` | Fetch the list of currently available Blablador model ids (live call). |
| `list_chatai_models(chatai_key)` | Fetch the list of currently available Chat AI model ids (live call). |
| `list_ollama_models(ollama_key)` | Fetch the list of currently available DLR Ollama model ids (live call). |
| `_load_model_list_cache()` | — |
| `_save_model_list_cache()` | — |
| `get_cached_models(service)` | The stored model list for a service (no network), or ``[]`` if none. |
| `_fetch_models_live(service)` | Do the actual /models network call for a service. |
| `list_available_models(service, force_refresh)` | Return the model ids for 'BlaBla', 'Chat AI' or 'DLR Ollama'. |
| `probe_available_services(force_refresh)` | The services that are usable: an API key is stored **and** a model list is known (from the cache, or fetched once on a miss). |
| `select_model_interactive(service)` | Show the stored model list for a service and let the user pick one (stored for the session and returned). |
| `_filter_embedding_models(models)` | Return only the models that look like embedding models: id/name contains 'embed' (case-insensitive) or is an e5-family model (Chat AI's embedding ids, e.g. |
| `list_embedding_models(service, force_refresh)` | Return the embedding-model ids for a service ('BlaBla', 'Chat AI' or 'DLR Ollama'): the cached full model list (or fetched once on a miss), filtered to the embedding models (id/name contains 'embed', or e5-*). |
| `get_default_embedding_model(service, preferred)` | Resolve which embedding model to use for `service`. |
| `set_selected_embedding_model(service, model)` | Set the embedding model to use for a service for the rest of the session. |
| `set_embedding_backend(method, service)` | Set the session-wide embedding backend used by vector_db_updater's vectorize_strings()/search_similar() defaults (cosine evaluation, RAG vector-DB builds, vector queries). |
| `get_embedding_backend()` | Return the current session embedding-backend selection (copy). |
| `_parse_embedding_response(result, expected)` | Pull the vectors out of an /embeddings response (OpenAI-compatible or native Ollama shape). |
| `_embed_in_halves(texts, embed_fn, label)` | Embed a pool of strings with as few calls as possible: try the WHOLE pool in one call first; if that fails (server limit, timeout, OOM …), split the pool in half and retry each half the same way, recursively, until the pieces go through. |
| `get_embedding(text, service, model, api_key, timeout)` | Get embedding vector(s) for `text` from the given service's OpenAI-compatible /embeddings endpoint. |
| `save_embedding_result(result, out_dir, prefix)` | Save an embedding result (as returned by get_embedding) to a timestamped JSON file. |
| `last_token_pool(last_hidden_states, attention_mask)` | Pool the last non-padding hidden state per sequence (Qwen3-embedding style pooling). |
| `load_embedding_model_and_tokenizer(local_dir)` | Load the local HuggingFace embedding model + tokenizer from disk (GPU if available, else CPU fallback). |
| `_get_embedding_model_and_tokenizer()` | Load and cache the local embedding model/tokenizer on first use. |
| `vectorize_strings_local(input_strings, max_length, batch_size)` | Embed a list of strings using the local GPU/CPU HuggingFace model (Qwen/Qwen3-Embedding-8B by default, same weights as before). |
| `count_tokens(messages, response_text, model)` | Calculates input and output token counts. If tokenisation fails for any reason, returns ("NA", "NA"). |
| `log_llm_interaction(model, service, messages, response_text, time_taken)` | — |
| `Ollama_ask_llm(prompt, sys_prompt, temperature, max_tokens, model, timeout)` | — |
| `cache_blablador_models(blablador_key, cache_file)` | Fetch Blablador models and cache RAW response to JSON file. |
| `find_best_blablador_model(blablador_key)` | Dynamic model selection from Blablador API. |
| `blabla_ask_llm(prompt, sys_prompt, temperature, max_tokens, blablador_key, model, timeout)` | Query Blablador LLM with the selected (or default) model. |
| `chatai_ask_llm(prompt, sys_prompt, temperature, max_tokens, chatai_key, model, timeout)` | Query Chat AI (academiccloud) with the selected (or default) model. |
| `hf_pipeline_with_Lamma()` | — |
| `Local_Model_call(prompt, sys_prompt)` | — |
| `llm_call(prompt, system_prompt, service, model, allow_fallback, timeout)` | Chat call with native timeouts, bounded retries, and a *recorded* fallback. |
| `blabla_ask_embedding(texts)` | Embed via Blablador using the session-selected embedding model. |
| `chatai_ask_embedding(texts)` | Embed via Chat AI using the session-selected embedding model. |
| `Ollama_ask_embedding(texts)` | Embed via DLR Ollama using the session-selected embedding model. |
| `local_ask_embedding(texts)` | Embed via the local HuggingFace model; same result shape as get_embedding(). |
| `_normalize_embedding_service_code(service)` | Map 'B'/'BlaBla'/'C'/'Chat AI'/'O'/'DLR Ollama'/'L'/'local' (any case) to 'b'/'c'/'o'/'l'. |
| `embedding_call(texts, service, model, timeout, allow_fallback)` | Get embeddings with native timeouts + bounded retries (inside get_embedding via _post_with_retries), mirroring llm_call(): 'B' -> Blablador /embeddings (never cross-service falls back). |

### `src/alr/common/sections.py`

alr.common.sections ==================== Single source of truth for the ALR "section" concept (Research Problem, Objective, Methodology, Conclusion, Results, Research Areas, Key Concepts).  
*484 lines · 2 classes · 14 functions*

**class `SectionField` (str, Enum)** — Which attribute of a section this field refers to on Vec_DB_Manager.

**class `SectionSpec`** — Everything needed to locate every representation of one section.

| Function | Purpose |
| --- | --- |
| `get_sections_map(vdb, fields, only)` | Generic replacement for every `_build_sections_map*` variant. |
| `get_master_sections_map(vdb, master_excel_path, only)` | Replacement for Master_excel_DB_Builder._build_sections_Master_map(VDB, master_excel_path). |
| `get_eval_sections_map(vdb)` | Replacement for Data_evaluator's inline SECTION_MAP. |
| `build_sections_map(vdb, only)` | Was: RAG_BUILDERs/Text_DB_updater.py :: _build_sections_map(VDB) |
| `build_sections_map_vdb(vdb, only)` | Was: RAG_BUILDERs/DB_Manager.py :: _build_sections_map_VDB(VDB) |
| `build_sections_map_full(vdb, only)` | Was: RAG_BUILDERs/querry_excecuter.py :: _build_sections_map_full(VDB) |
| `build_sections_map_ra_kc(vdb)` | Was: RAG_BUILDERs/querry_excecuter.py :: _build_sections_map_RA_KC(VDB) |
| `build_sections_map_vdb_excel(vdb, only)` | (BIN, EXCEL) variant of build_sections_map_vdb: the vector sync now embeds the same positional Content column the query executor aligns against, instead of the section JSON which could drift ahead of it. |
| `build_sections_master_map(vdb, master_excel_path, only)` | Was: RAG_BUILDERs/Master_excel_DB_Builder.py :: _build_sections_Master_map(VDB, master_excel_path) |
| `build_sections_eval_map(vdb)` | Was: Analysis_Evaluation/Data_evaluator.py :: inline SECTION_MAP |
| `is_literal_match_key(key)` | True when ``key`` keeps the whole-string substring check. |
| `build_intro_sections_eval_map(vdb)` | Introduction counterpart of :func:`build_sections_eval_map`: ``{intro_section_key: (eval_excel_path, intro_section_key)}``. |
| `build_rescon_sections_eval_map(vdb)` | Results & Conclusion counterpart of :func:`build_sections_eval_map`: ``{rescon_section_key: (eval_excel_path, rescon_section_key)}``. |
| `build_metric_workbooks_map(vdb, target)` | Return ``{metric_kind, "overview" or "details": path}`` for a target ("abstract", "intro" or "rescon"), resolved against a Vec_DB_Manager instance. |

### `src/alr/common/skiplog.py`

alr.common.skiplog ================== One shared shape for the "could not be processed" logs every long pass writes.  
*59 lines · 0 classes · 2 functions*

| Function | Purpose |
| --- | --- |
| `skip_row(stage, error, space, uuid, title, filename, sections)` | One row of a 'not added' log: what failed, on which document, and when. |
| `append_skiplog(log_path, skipped_rows)` | Append this run's failures to a skip log (never overwrites earlier runs). |

### `src/alr/common/sql_store.py`

alr.common.sql_store ==================== A lightweight SQLite store for analyzed-document *summaries*, kept alongside the existing file-based storage (see :mod:`alr.common.file_manager`).  
*1179 lines · 2 classes · 20 functions*

**class `_ClosingConnection` (sqlite3.Connection)** — A connection that also **closes** when its ``with`` block ends.

| Method | Purpose |
| --- | --- |
| `__exit__(self, exc_type, exc, tb)` | — |

**class `AnalyzedDataStore`** — CRUD access to the analyzed-document SQLite database.

| Method | Purpose |
| --- | --- |
| `__init__(self, db_path)` | — |
| `_connect(self)` | — |
| `init_db(self)` | Create/migrate the schema — but only actually write when something is missing. |
| `upsert_document(self, record)` | Insert or update a document by uuid. ``created_at`` is set only on the first insert. |
| `update_document(self, uuid, fields)` | Update selected columns for a document (used by the editable review view). |
| `delete_document(self, uuid)` | — |
| `list_documents(self, search)` | Return documents (as dicts) ordered by most recent, optionally filtered. |
| `get_document(self, uuid)` | — |
| `find_document(self, filename, source_folder)` | Return the newest document row for ``filename`` (optionally restricted to one storage space), or None. |
| `count(self)` | — |
| `available_fields()` | All selectable columns for a custom overview (uuid always available). |
| `list_source_folders(self)` | — |
| `_filter_clause(filters)` | Build a WHERE clause (without the WHERE keyword) + params from filters. |
| `topic_counts(self, column, filters)` | Count documents per *individual* classification topic by splitting the comma-joined ``classification`` / ``abstract_classification`` column. |
| `build_overview(self, fields, filters)` | Return a custom overview: the chosen ``fields`` (defaults to all), filtered by an optional ``filters`` dict with any of: source_folder (exact), publication_year (exact), publication_type (exact), research_area (substring, case-insensitive). |
| `grouped_overview(self, group_by, filters)` | Aggregated overview: count of documents grouped by ``group_by`` (a column), honouring the same filters as build_overview. |
| `stats(self)` | High-level cross-space statistics over the whole database. |
| `run_select(self, sql, max_rows)` | Run a single read-only SELECT/WITH query and return (columns, rows). |
| `save_template(self, name, spec)` | — |
| `list_templates(self)` | — |
| `get_template(self, name)` | — |
| `delete_template(self, name)` | — |
| `_real_value(val)` | A usable metadata cell value, or None for empty/placeholder cells. |
| `merge_metadata_workbook(self, df)` | Merge DOI/publication metadata from a workbook DataFrame into existing documents — same fill-if-empty semantics as :meth:`merge_download_log`, but covering the ``*_DOI_Metadata.xlsx`` / ``publications_metadata.xlsx`` column layouts (see ``METADATA_TO_DB``). |
| `merge_download_log(self, df)` | Merge bibliographic data from a download-log DataFrame into existing documents, matching the log's ``File_Name`` to a document ``filename``. |

| Function | Purpose |
| --- | --- |
| `_slug(section_key)` | Turn a section key ("Research Problem") into a column name ("research_problem"). |
| `_prefixed_slug(prefix, section_key)` | Column name for a non-abstract attribute ("Gaps & Limitations" -> "intro_gaps_limitations"). |
| `sanitize_column_name(name)` | Turn a user-supplied topic name into a safe SQLite column name: lower-cased, non-alphanumerics collapsed to underscores. |
| `register_custom_column(name, db_path)` | Register a user-defined enrichment column (e.g. |
| `_read_json(path)` | Load a JSON file, returning (dict_or_None, raw_text_or_None). |
| `_truthy_classification(val)` | Interpret one classification cell as a boolean. |
| `_enable_wal(conn, db_path)` | Put the database in WAL mode once per file, if the file system allows it. |
| `describe_record_data(record)` | Which analyzed data one record actually carries, as a short human phrase ("abstract + introduction + results & conclusion attributes"). |
| `_apply_section_values(record, data, columns)` | Copy one analysis JSON's attribute values into their record columns. |
| `set_default_search_root(path)` | Set (or clear, with None/'') the fallback root for analysis JSONs. |
| `get_default_search_root()` | — |
| `_analysis_json_names(uuid)` | ``{source: filename}`` of the three analysis JSONs of one document. |
| `_locate_missing_analysis_jsons(uuids, manager, search_root)` | ``{filename: path}`` for the analysis JSONs the space itself does not hold, found under ``search_root``. |
| `_analysis_json_path(manager, folder, uuid, source, located)` | The document's analysis JSON in the space, else the located fallback. |
| `_record_from_registry_row(row, manager, enrichment_data, located_jsons)` | Build a documents record from one registry row + the on-disk JSON files. |
| `sync_storage_to_sql(manager_or_folder, db_path, progress_callback, search_root)` | Sync a storage space's analysis output into the database. |
| `_merge_space_metadata_files(store, manager)` | Merge every ``*_DOI_Metadata.xlsx`` (managed DOI workbook folder) and ``*publications_metadata.xlsx`` (anywhere in the space) into the store — newest file first, fill-if-empty — so the freshest recorded metadata wins but nothing already in SQL is overwritten. |
| `_merge_space_evaluation_overviews(store, manager)` | Rebuild evaluation summaries from the newest evaluation-overview workbook (``{space}/Abstract_DB/Abstract_Overview_folder/*_Abstract_Eval_Overview.xlsx`` and the Introduction counterpart) and store them fill-if-empty into the ``evaluation_json``/``evaluation_score`` (resp. |
| `_merge_space_classification_files(store, manager)` | Merge every managed publication-classification workbook in the space's ``Publication_Classification_Files`` folder into SQL -- newest file per target column first, fill-if-empty -- so classification tags recorded on disk populate the database when a space is linked into a fresh DB (mirrors :func:`_merge_space_metadata_files` / :func:`_merge_space_evaluation_overviews`). |
| `sync_one_document(manager_or_folder, filename, db_path, search_root)` | Sync a single document (matched by ``filename``) from a storage space's registry into the SQLite store. |

### `src/alr/common/storage_scanner.py`

alr.common.storage_scanner ========================== Discover DataAnalyzeManager "storage spaces" and download logs under a folder.  
*92 lines · 1 classes · 2 functions*

**class `StorageSpace`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `name(self)` | — |

| Function | Purpose |
| --- | --- |
| `detect_storage_spaces(root)` | Recursively find every DataAnalyzeManager storage space under ``root``. |
| `find_download_logs(root)` | Find all bibliographic/metadata workbooks under ``root`` that can be merged into the review database: download logs (``*_download_log*.xlsx``, see File_Downloader._build_paths), managed DOI workbooks (``*_DOI_Metadata.xlsx``) and publication-metadata exports (``*publications_metadata.xlsx``). |

## Desktop UI

*The Tkinter main window, the Review tool and their shared widgets.* — `alr/ui/desktop/`

### `src/alr/ui/desktop/_async.py`

alr.ui.desktop._async ====================== Shared UI plumbing used by both the main window and the Review tool: * :class:`ProgressDialog` - a small status/progress/Cancel dialog (modal or not).  
*310 lines · 1 classes · 2 functions*

**class `ProgressDialog`** — A small dialog with a status message, progress bar and Cancel.

| Method | Purpose |
| --- | --- |
| `__init__(self, master, title, on_cancel, modal)` | — |
| `_cancel(self)` | — |
| `apply(self, done, total, text)` | — |
| `close(self)` | — |

| Function | Purpose |
| --- | --- |
| `make_scrollable_tab(notebook, title)` | Add a notebook tab whose content can grow past the window height: the content lives inside a vertical-scroll canvas. |
| `run_threaded(host, work, title, result_word, on_success, on_cancelled, modal, cleanup, status, after)` | Run ``work(progress, should_cancel)`` on a background thread with a progress dialog. |

### `src/alr/ui/desktop/activity_bar.py`

alr.ui.desktop.activity_bar ========================== An always-visible strip that shows live model activity by polling ``alr.common.activity_monitor``: which chat/embedding model is running right now (with an animated bar), plus running totals — tokens processed by text-generation models and vectors produced by embedding models.  
*80 lines · 1 classes · 0 functions*

**class `ActivityBar`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, parent, interval_ms)` | — |
| `pack(self, **kw)` | — |
| `grid(self, **kw)` | — |
| `format(snap, now)` | — |
| `_tick(self)` | — |

### `src/alr/ui/desktop/chat_view.py`

alr.ui.desktop.chat_view ======================== A small, self-contained **AI chat** window shared by the main tool and the Review tool.  
*503 lines · 3 classes · 5 functions*

**class `ChatSession`** — Conversation state + the LLM call. Holds no Tk objects.

| Method | Purpose |
| --- | --- |
| `__init__(self, service_code, model, history_turns)` | — |
| `_convo_text(self, user_text)` | — |
| `ask(self, user_text, context_text)` | Send one turn; append both sides to history; return the response text. |

**class `DocumentContextController`** — Builds the left-hand context panel for the Review tool's chat: a searchable multi-select list of documents and a set of attribute checkboxes.

| Method | Purpose |
| --- | --- |
| `__init__(self, store)` | — |
| `build_panel(self, parent)` | — |
| `_reload(self)` | — |
| `selected_uuids(self)` | — |
| `selected_attrs(self)` | — |
| `_update_status(self)` | — |
| `get_context(self)` | — |

**class `ChatWindow`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, master, title, context_controller, context_token_budget)` | — |
| `_service_code(self)` | — |
| `_on_service_change(self)` | — |
| `_choose_model(self)` | — |
| `_append(self, who, text, error)` | — |
| `_on_return(self, event)` | — |
| `_clear(self)` | — |
| `_send(self)` | — |

| Function | Purpose |
| --- | --- |
| `_get_encoding()` | Lazily load a tiktoken encoding once; False if tiktoken is unavailable. |
| `estimate_tokens(text)` | Approximate token count of ``text`` (tiktoken, or ~4 chars/token). |
| `truncate_to_tokens(text, max_tokens)` | Return ``(text, was_truncated)`` trimmed to at most ``max_tokens`` tokens. |
| `format_document_context(store, uuids, attributes)` | Build a plain-text context block from selected documents and attributes. |
| `open_chat_window(master, title, context_controller, context_token_budget)` | Open the AI chat window; returns the Toplevel. |

### `src/alr/ui/desktop/main_window.py`

The main desktop application: all five tabs, the shared console and every action handler.  
*3961 lines · 2 classes · 1 functions*

**class `CustomTerminalText` (tk.Text)** — Custom Text widget to redirect stdout/stderr to the GUI window.

| Method | Purpose |
| --- | --- |
| `__init__(self, *args, **kwargs)` | — |
| `_strip_ansi(self, line)` | — |
| `_filter_noise(self, chunk)` | Drop developer-noise lines from ``chunk`` when not in verbose mode. |
| `_configure_ansi_tags(self)` | — |
| `write(self, string)` | — |
| `_active_tags(self)` | — |
| `_apply_sgr(self, params)` | Update the active colour state from one SGR escape's parameters. |
| `_insert_ansi(self, text)` | Insert ``text``, converting ANSI SGR codes into Tk tag styling. |
| `_trim(self)` | Keep the console bounded. An unbounded Text widget is what makes a long, chatty pass (a DB build prints per section per document) freeze the whole window: every insert into a several-hundred-thousand-line widget gets slower, on the main thread. |
| `_poll_write_queue(self)` | — |
| `flush(self)` | — |

**class `AutomatedLiteratureUI` (tk.Tk)** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self)` | — |
| `_create_widgets(self)` | — |
| `_refresh_provider_status(self, provider_code)` | Update the top status strip with the active LLM provider, its selected model and whether an API key is stored. |
| `_select_tab_by_prefix(self, prefix)` | Bring the notebook tab whose title starts with ``prefix`` to the front, un-hiding it first if it is one of the '+'-tucked optional tabs. |
| `_setup_optional_tabs(self)` | Tuck the Evaluation & Enrichment tabs behind a trailing '+' tab. |
| `_hidden_optional_tabs(self)` | [(tab_id, title), …] for optional tabs currently hidden. |
| `_on_tab_changed(self, event)` | Bounce off the '+' tab (never stay on it) and pop the add-menu. |
| `_open_add_tab_menu(self)` | Menu of the hidden optional tabs, opened from the '+' tab. |
| `_reveal_tab(self, tab_id)` | Un-hide an optional tab and bring it to the front. |
| `_on_tab_right_click(self, event)` | Offer to hide (close) a right-clicked optional tab, browser-style. |
| `_hide_tab(self, tab_id)` | Hide an optional tab, moving selection off it first if needed. |
| `_show_current_tab_help(self)` | Show plain-language help for whichever tab is currently open. |
| `_open_ai_chat(self)` | Open the shared AI chat window (plain chat. |
| `_toggle_verbose_log(self)` | Show or hide the developer-noise lines in the console log. |
| `_toggle_console(self)` | Fold the console log away, or drop it back down. |
| `_apply_console_visibility(self)` | Add or remove the log pane, and label the toggle for what it will do. |
| `_refresh_console_toggle(self)` | Label the toggle, flagging output that arrived while it was folded. |
| `_note_console_output(self)` | The log received output — mark the toggle when nobody can see it. |
| `_clear_console(self)` | Empty the console log (the widget only; nothing on disk changes). |
| `_set_last_result(self, text, ok)` | Echo the last action's outcome on the one-line status label above the console (green when ok, red on failure/cancel). |
| `_browse_active_space(self)` | — |
| `_browse_into_var(self, var, title)` | Pick a directory and store it in a StringVar. |
| `_prompt_search_root(self)` | Main-thread handler (called via ``_run_threaded``'s ``ask``) that asks the user to pick the folder to search for the analysis JSONs and remembers it for next queries. |
| `_set_active_space(self, path)` | Point the shared active space at ``path`` (updates every tab whose 'Use active space' toggle is on). |
| `_register_active_space_consumer(self, toggle_var, entry, side_widgets)` | Register a tab's storage entry so it mirrors the active space while its ``toggle_var`` is on (entry + browse buttons disabled), and becomes an editable override when the toggle is off. |
| `_apply_active_space(self)` | Push the active-space path into every consumer whose toggle is on. |
| `_set_entry_text(entry, text)` | Set an Entry's text regardless of its current state (StringVar-backed entries update via their variable. |
| `_session_state_path()` | — |
| `_cacheable_items(self)` | Every user-input widget/variable to persist, keyed by its attribute name. |
| `_restore_session_state(self)` | Pre-fill every cached tab input from the last session's cache file. |
| `_save_session_state(self)` | Cache every user input across all tabs to the session cache file. |
| `_on_close(self)` | — |
| `_build_collect_tab(self)` | — |
| `_collection_root(self)` | Resolve the CollectionManager root: a ``collection`` sub-folder of the active storage space (when the toggle is on), or of a custom folder (when it's off). |
| `_ensure_collection_manager(self)` | Make sure ``self.CM`` exists and reflects the current RA/RQ/service entries. |
| `_current_scope(self)` | The refined scope if the user chose to use it, else ''. |
| `_generate_scope_action(self)` | — |
| `_make_check_table(self, parent, columns, height, numeric_sort)` | Build a Treeview whose first column is a click-to-toggle checkbox, followed by ``columns`` = [(id, heading, width), ...]. |
| `_toggle_all_rows(self, tree)` | — |
| `_sync_select_all(self, tree)` | Mirror the per-row state onto the Select-all checkbutton. |
| `_sort_numeric_column(self, tree, column)` | Reorder the rows by ``column``'s numeric value, toggling ascending / descending on each heading click. |
| `_tree_toggle_check(self, tree, event)` | — |
| `_tree_edit_cell(self, tree, event)` | In-place editing of the text column (last column) on double-click. |
| `_checked_rows(self, tree, column)` | Values of ``column`` for every checked row, in table order. |
| `_remove_selected_rows(self, tree)` | — |
| `_add_manual_keywords(self)` | — |
| `_add_manual_phrases(self)` | — |
| `_import_keywords_action(self)` | Load a previous keywords file into the table: list ALL unique keywords it recorded (LLM-suggested + user-added + selected across every timestamped entry) and check exactly the ones from the last selection. |
| `_import_phrases_action(self)` | Load search phrases (with ranks, when present) from a previously generated workbook into the phrase table for re-display and search. |
| `_suggest_keywords_action(self)` | LLM keyword suggestions land directly in the keyword table (checked), replacing the old refinement pop-up. |
| `_keyword_provenance(self)` | (suggested, user_added, selected) keyword lists from the current table, split by their recorded origin. |
| `_process_keywords_action(self)` | — |
| `_refresh_phrase_ranks(self)` | Radio/spinbox hook: re-rank the phrase table when generated phrases are present. |
| `_refresh_phrase_table(self)` | Fill the search-phrase table from the generated phrase workbook, sorted by the selected ranking strategy; the top-N (phrase count spinbox) rows come pre-checked. |
| `_execute_search_strategy(self, choice_mode)` | — |
| `_on_publications_collected(self, pub_count)` | After a publication search: surface the collected count in the UI and offer to classify the list against the selected keywords. |
| `_classify_publications_action(self)` | Classify the collected publications against the checked keywords via the custom classifier, storing the result in the collection space. |
| `_build_analyze_tab(self)` | — |
| `_toggle_analyze_path_btn(self)` | — |
| `_analyze_space_folder(self)` | The storage space Tab 1 works on (custom path, else the default). |
| `_resolve_missing_attributes_action(self)` | Finish an already-analyzed storage space **without any PDFs**. |
| `_run_analysis_action(self)` | — |
| `_analysis_summary(counts, cancelled)` | Report what the analysis pass actually did: how many documents were analyzed, how many failed inside the processor (recorded in the space's failure log) and how many were skipped outright (recorded in ``Analysis_not_added.xlsx``). |
| `_run_threaded(self, work, title, result_word, on_success, on_cancelled)` | Run a long pass on a background thread with a non-modal progress dialog. |
| `_build_visualize_tab(self)` | — |
| `_make_section_checkbox_grid(self, parent, default)` | One checkbox per RAG section (abstract + Introduction + Results & Conclusion attributes), grouped by their analysis source. |
| `_finalization_gap_dialog(self, gaps, timeout)` | Ask, in ONE modal, what to do about each enrichment stage that is still missing from the SQL database after a run (see ``analysis_precheck.compute_space_gaps``). |
| `_pick_attributes_dialog(self, title, default_keys)` | Modal checkbox picker over every analyzed attribute (abstract + Introduction + Results & Conclusion). |
| `_build_common_db_frame(self, tab)` | Common Database frame: combine the text + vector DBs of several storage spaces into ONE queryable location (the RAG counterpart of the app-wide SQL database). |
| `_common_db_config_path()` | — |
| `_load_common_db_config(self)` | — |
| `_save_common_db_config(self)` | — |
| `_add_common_source(self)` | — |
| `_scan_common_sources(self)` | — |
| `_remove_common_sources(self)` | — |
| `_build_common_db_action(self)` | — |
| `_rebuild_section_dbs_action(self)` | Rebuild chosen attributes' section databases: their Excel/JSON from the per-document analysis JSONs, then their FAISS index from that Excel. |
| `_run_visualization_query_action(self)` | — |
| `_make_scrollable_tab(self, title)` | Add a notebook tab whose content can grow past the window height (thin wrapper over the shared :func:`alr.ui.desktop._async.make_scrollable_tab`). |
| `_build_shared_eval_inputs(self, tab)` | Build one copy of the shared Storage-Space / LLM / Embedding inputs. |
| `_build_evaluation_tab(self)` | — |
| `_build_enrichment_tab(self)` | — |
| `_run_evaluation_action(self)` | Run the selected evaluation types over the shared storage space, against the chosen target data (abstract / introduction / both). |
| `_run_custom_classification_action(self)` | Classify every document in the shared storage space against the user-defined tags: the classification prompt is generated from the tags, results go to a dated '{date}_{Topic}_Classification.xlsx' workbook and to a SQL column named after the topic tag. |
| `_run_doi_extraction_action(self)` | — |
| `_run_storage_pass(self, mode)` | — |
| `_classify_title_action(self)` | — |
| `_question_score_action(self)` | — |
| `_compute_metrics_action(self)` | — |
| `_manage_api_keys_action(self)` | Modal dialog to view/enter and persist the provider API keys. |
| `_ensure_api_key(self, provider_code)` | Ensure a key exists for the selected provider (code or display name). |
| `_browse_file(self, target_entry)` | — |
| `_browse_folder(self, target_entry)` | — |
| `_choose_model_action(self, provider_code)` | Show the model list for the selected provider ('B' = Blablador, 'C' = Chat AI, 'O' = DLR Ollama) and let the user pick one, stored as the session model for all subsequent LLM calls. |
| `_build_embedding_selector(self, parent)` | Build one 'Embedding Engine' selector row inside `parent`. |
| `_apply_embedding_backend(self)` | Push the widgets' state into the session-wide embedding backend and enable/disable the API-only controls (service box + model button). |
| `_choose_embedding_model_action(self)` | Show the embedding-model list for the selected embedding service ('B' = Blablador, 'C' = Chat AI, 'O' = DLR Ollama) and let the user pick one, stored as the session embedding model. |

| Function | Purpose |
| --- | --- |
| `_provider_code(value)` | Accept a provider code ('B') or a display label ('Blablador') -> code. |

### `src/alr/ui/desktop/review_app.py`

alr.ui.desktop.review_app ======================== Standalone Review application.  
*2041 lines · 1 classes · 11 functions*

**class `ReviewApp`** — Builds the whole review tool inside a given Tk container (window).

| Method | Purpose |
| --- | --- |
| `__init__(self, container)` | — |
| `_open_ai_chat(self)` | Open the AI chat with a document-data context picker (single or many documents, chosen attributes) sourced from the review database. |
| `_build_spaces_tab(self, nb)` | — |
| `_scan_folder(self)` | — |
| `_selected_space(self)` | — |
| `_run_threaded(self, work, title, result_word, on_success)` | Run a long pass on a background thread with a modal progress dialog. |
| `_session_state_path()` | The main tool's session cache — the search root is remembered there so setting it in either tool serves both. |
| `_load_saved_search_root(self)` | — |
| `_apply_search_root(self)` | Point the storage->SQL sync at the current root and remember it. |
| `_browse_search_root(self)` | — |
| `_link_selected(self)` | — |
| `_link_all(self)` | — |
| `_doi_selected(self)` | — |
| `_classify_selected(self)` | — |
| `_evaluate_selected(self)` | — |
| `_open_selected_space(self)` | — |
| `_import_download_log(self)` | Merge the selected bibliographic workbook into the database. |
| `_build_documents_tab(self, nb)` | — |
| `_build_inspector_tab(self, nb)` | — |
| `_inspector_pick_space(self)` | — |
| `_inspector_search(self)` | — |
| `_inspector_show_selected(self)` | — |
| `_inspector_fill_tree(self, view_rows)` | — |
| `_inspector_show_value(self)` | — |
| `_inspector_clear_detail(self)` | — |
| `_inspector_open_pdf(self)` | — |
| `_inspector_locate_pdf(self)` | — |
| `_inspector_pick_match(self, matches)` | Modal chooser when the recursive search finds several copies. |
| `_build_database_tab(self, nb)` | — |
| `_fill_tree(tree, columns, rows, limit)` | — |
| `_refresh_database_tab(self)` | — |
| `_load_db_table(self)` | — |
| `_run_sql_query(self)` | — |
| `_export_sql_result(self)` | — |
| `_export_db_columns(self)` | Export database documents to Excel by ticking columns — no SQL needed. |
| `_build_overviews_tab(self, nb)` | — |
| `_selected_fields(self)` | — |
| `_current_filters(self)` | — |
| `_group_by(self)` | — |
| `_overview_data(self)` | Return (columns, rows) for the current overview (grouped or flat). |
| `_preview_overview(self)` | — |
| `_export_overview(self, kind)` | — |
| `_export_rows(self, cols, rows, default_name, kind)` | — |
| `_render_chart(self, labels, values, title)` | Draw a bar/pie chart of ``labels``/``values`` into the chart frame. |
| `_show_chart(self)` | — |
| `_show_topic_counts(self)` | Per-topic overview: split the classification tags and count per topic. |
| `_export_chart(self)` | — |
| `_current_spec(self)` | — |
| `_apply_spec(self, spec)` | — |
| `_reload_templates(self)` | — |
| `_save_template(self)` | — |
| `_load_template(self)` | — |
| `_delete_template(self)` | — |
| `_build_from_description(self)` | — |
| `_build_section_editor_tab(self, nb)` | Host the section JSON editor here (moved out of the main tool). |
| `_build_help_tab(self, nb)` | — |
| `_build_data_files_tab(self, nb)` | — |
| `_df_set_spaces(self, folders, scan_root)` | Load one or several storage spaces into the Data Files tab. |
| `_df_set_space(self, folder)` | — |
| `_df_select_space(self)` | — |
| `_df_select_folder(self)` | Scan a selected folder for storage spaces — same recognition as the Storage Spaces tab — and list the data files of ALL spaces found. |
| `_df_use_selected_space(self)` | — |
| `_df_scan(self)` | — |
| `_df_render_scan(self)` | Rebuild the Data Files checkbox grid + preview from the last scan. |
| `_df_toggle_all(self, value)` | — |
| `_df_selected_paths(self)` | — |
| `_df_merge(self)` | Merge selected files per fine 'same type' key into a user-chosen folder. |
| `_df_preview_group(self)` | — |
| `_df_export(self)` | Export the currently-previewed merged group (user picks path + format). |
| `_df_check_sql(self)` | Compare each merged group against the SQL database and, where it isn't already reflected, offer to update it. |
| `_refresh_all(self)` | — |

| Function | Purpose |
| --- | --- |
| `nl_to_overview_spec(description, fields)` | Turn a plain-English overview request into a spec dict via the LLM: {"fields": [...], "group_by": <col\|None>, "filters": {...}}. |
| `_df_truthy(val)` | True for affirmative classification cells (bool / 1 / 'True' / 'yes'). |
| `discover_space_data_files(manager)` | Return an ordered ``{category: [file paths]}`` mapping for one storage space (a DataAnalyzeManager). |
| `data_file_type_key(path)` | Fine-grained 'same type' key for a data file, used to group files that may safely merge together. |
| `_read_table(path)` | Read the primary per-document sheet of a workbook as a DataFrame. |
| `pick_key_column(df)` | Pick the per-document key column (UUID > File_Name > filename), or None. |
| `merge_data_files(paths)` | Merge several same-type workbooks into one per-document table: concatenate (oldest -> newest) and keep the newest row per document key, so repeated dated runs collapse to one row each -- the SQL-like shape. |
| `classification_topic_columns(df)` | Columns of a classification table that represent topics (booleans). |
| `classification_tags(row, topic_cols)` | Comma-joined list of true topics for one classification row (SQL summary). |
| `sql_target_for_type(type_key)` | Map a fine type key to how its merged data lands in SQL: ('metadata', None) -> DOI/publication metadata (merge_metadata_workbook) ('classification', col) -> a classification summary column (fill-if-empty) ('sync', None) -> reflected by syncing the space (registry/log/eval) ('skip', None) -> not a per-document table (question-scored) |
| `open_review_app(master)` | Open the Review tool in its own window (Toplevel child, or standalone root). |

### `src/alr/ui/desktop/review_view.py`

alr.ui.desktop.review_view ========================== An editable "Review Data" view for browsing and curating analyzed documents stored in the SQLite database (:mod:`alr.common.sql_store`).  
*313 lines · 1 classes · 2 functions*

**class `ReviewDataView`** — Treeview list + editable detail panel over the analyzed-document store.

| Method | Purpose |
| --- | --- |
| `__init__(self, parent)` | — |
| `_build(self)` | — |
| `refresh(self)` | Reload the document list from the DB (respecting the search box). |
| `_clear_search(self)` | — |
| `_on_select(self, event)` | — |
| `_decode_section(key, value)` | DB value -> text shown in the editor (lists become one-per-line). |
| `_encode_section(key, text)` | Editor text -> DB value (list sections become a JSON array). |
| `_save(self)` | — |
| `_sync_action(self)` | — |
| `_current_rows(self)` | The documents currently shown (respects the search filter). |
| `_export_csv(self)` | — |
| `_export_excel(self)` | — |
| `_open_pdf(self)` | — |
| `_open_json(self)` | — |
| `_delete(self)` | — |

| Function | Purpose |
| --- | --- |
| `open_path(path)` | Open a file or folder with the OS default application. |
| `open_review_window(master)` | Open the review view in its own top-level window (for side-by-side use). |

### `src/alr/ui/desktop/section_rewriter_view.py`

Section Editor panel: review and correct a document's identified section text.  
*280 lines · 1 classes · 1 functions*

**class `JSONRestructurerUI`** — _no docstring_

| Method | Purpose |
| --- | --- |
| `__init__(self, root)` | — |
| `create_widgets(self)` | — |
| `browse_file(self)` | — |
| `load_json_data(self)` | — |
| `refresh_dropdown(self)` | Refreshes the drop-down menu choices matching current dataset state |
| `has_unsaved_changes(self)` | Checks if the currently visible text differs from the snapshot data |
| `check_and_prompt_unsaved_changes(self)` | Prompts user to save changes if deviations are found. |
| `on_section_dropdown_switch(self, event)` | Triggered when the user picks a different item in the combobox. |
| `load_section_into_ui(self, index)` | Populates the text areas with data from memory and saves snapshots for comparison. |
| `commit_current_ui_to_memory(self)` | Saves current fields down into the target dictionary element index context |
| `add_empty_section(self)` | Appends a fresh section to the tracking array list after checking current modifications |
| `confirm_and_save(self)` | — |

| Function | Purpose |
| --- | --- |
| `open_section_editor_window(master)` | Open the JSON section editor in its own top-level window. |

## Command-line UI

*Terminal front-ends for the same pipeline.* — `alr/ui/cli/`

### `src/alr/ui/cli/Data_analysis_UI.py`

Terminal menu for the analysis stage.  
*129 lines · 1 classes · 2 functions*

**class `PDFPathAnalysis`** — _no docstring_

| Function | Purpose |
| --- | --- |
| `analyse_pdf_input_path(path_input, recursive)` | Analyse a path to determine whether it is: - a single PDF file, or - a folder containing PDFs (optionally with subfolders) Returns a structured result including: - list of discovered PDF paths - PDF count - any errors encountered |
| `get_file_or_folder()` | — |

### `src/alr/ui/cli/collection_ui.py`

Terminal menu for the collection stage.  
*280 lines · 0 classes · 11 functions*

| Function | Purpose |
| --- | --- |
| `_define_id(CM)` | — |
| `get_RA_RQ(username)` | — |
| `get_llm_defined_scope(RA, RQ, service)` | — |
| `remove_by_indices(index_list, string_list)` | Removes elements from the string list based on the comma-separated indices provided in index_str. |
| `add_by_indices(index_list, string_list)` | — |
| `get_indicies(keywords_list)` | — |
| `get_input_keyword_list()` | — |
| `process_keyword_list(keywords_list)` | — |
| `get_keyword_list(CM, keywords_choice, service)` | — |
| `try_pub_search(CM, rank_col)` | — |
| `Process_search_phrases(CM)` | — |

### `src/alr/ui/cli/pipeline.py`

Terminal driver chaining collection -> analysis -> databases.  
*230 lines · 0 classes · 8 functions*

| Function | Purpose |
| --- | --- |
| `_choose_llm_service()` | — |
| `_user_check(text)` | — |
| `_choose_data_storage_path(username, type)` | — |
| `_define_data_storage_path(choice, type)` | — |
| `get_collection_ui()` | — |
| `get_data_analysis_ui()` | — |
| `get_visualize_ui()` | — |
| `select_functionality()` | — |

## Entry points

*Scripts that start the application.*

### `src/Main.py`

Legacy top-level entry point kept for the original CLI flow.  
*51 lines · 0 classes · 0 functions*

### `src/gui_main.py`

Desktop GUI entry point for the Automated Literature Review tool.  
*31 lines · 0 classes · 1 functions*

| Function | Purpose |
| --- | --- |
| `main()` | — |

### `src/review_main.py`

Standalone entry point for the Review tool.  
*27 lines · 0 classes · 1 functions*

| Function | Purpose |
| --- | --- |
| `main()` | — |
